import { SchedulerReservationReportsEntry } from "@/components/api/SchedulerPageEntries";

export default function ReservationMetricsPage() {
  return <SchedulerReservationReportsEntry view="metrics" />;
}
