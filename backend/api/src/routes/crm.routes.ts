// Rutas del módulo crm — pendiente de implementar
import { Router, type Router as ExpressRouter } from 'express'

const router: ExpressRouter = Router()

// TODO: registrar endpoints de crm

export default router
