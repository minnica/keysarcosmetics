// Rutas del módulo pos — pendiente de implementar
import { Router, type Router as ExpressRouter } from 'express'

const router: ExpressRouter = Router()

// TODO: registrar endpoints de pos

export default router
