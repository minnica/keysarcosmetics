// Rutas del módulo payroll — pendiente de implementar
import { Router, type Router as ExpressRouter } from 'express'

const router: ExpressRouter = Router()

// TODO: registrar endpoints de payroll

export default router
