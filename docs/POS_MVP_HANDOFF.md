# Relevo de implementación del MVP POS

> Documento vivo. Actualizado al refinar el plan el 2026-09-16; evidencia técnica del 2026-09-09.
> Rama: `feature/pos-frontend-clean`.
> Referencia: `12fb8045cc264b565cb6e764d95ad7b2447fbfa1`.

## Estado al relevo

- MVP online RV0–RV7 y RV9 bloqueante: implementado.
- RV10 técnica histórica de `db58fda`: 208/208 visual, builds, tipos, lint, unitarias e integración PostgreSQL en `PASS`; no atribuir esa matriz completa al candidato `06f9f6f`.
- Checkpoint actual: `RV10-P7`, bloqueado sobre el SHA inicial `6a6dfe3250dbdb338763c3a41aa32b5183877bd3`. Las guías visual/API ya identifican el candidato ejecutable `06f9f6f`, separan fixtures de persistencia y no ofrecen el recorrido funcional como disponible. La reauditoría confirmó que desde ese candidato sólo cambiaron documentos/estado, que el baseline durable conserva 208 hashes válidos y que las guardas del verificador siguen aprobando. El host continúa sin Windows/WSL/Wine y no se proporcionaron API, terminal, canal o acceso del PO autorizados; el check permanece abierto.
- Recuperación del ejecutor: el checkpoint RV10-P4 se validó y publicó en `d576b9ac91b2f8eb4012023be3ee685dfd3257bd`. La allowlist ahora reconoce su plantilla pública `apps/pos/.env.example` y los tests `.test.mjs`/`.test.ts` de helpers POS; los `.env` reales, otros ejemplos y secretos continúan bloqueados.
- Limpieza RV9-P4: el alta de personal ya no admite teclear códigos nuevos de 5–12 dígitos que REF y `posEmployeeWriteSchema` rechazan; se limita y normaliza a cuatro. La compatibilidad de lectura 4–12 para credenciales históricas se conserva en login, autorización y asistencia hasta que exista inventario/rotación real. No se retiraron contratos, providers, tablas, proyecciones, outboxes, datos ni fixtures.
- Siguiente acción general: ejecutar RV9-P1 con inventario autorizado antes de retirar R10–R12 y ejecutar RV9-P2 antes de cualquier retiro persistente; decidir RV6-P3 según el uso/rollback de `AGENDA_PROVIDER=http`. RV5-P1 sigue bloqueado hasta que exista una definición aprobada que identifique la unidad/composición/Agenda afectada. RV6-P1 y B01–B03 continúan aplazados; no implementar esas operaciones por inferencia.
- Pendiente visual explícito: RV3-B01 (copy de cierre/gasto). RV7-P1/R03 quedó cerrado sin modificar `REGISTRO MOCK · CASH MANAGER`. No equiparar 208 capturas históricas con cobertura universal del modo API.
- Backlog: cierre multi-proyección de revisiones complejas, administración avanzada de membresías e incidencias Scheduler, RV8/offline, hardware, migraciones/consumidores/limpieza y OP01–OP06 (piloto, respaldo, rollback y release). Agenda externa sólo si se habilita el proveedor HTTP o se conserva como rollback.
- Referencia reconfirmada por GitHub MCP el 2026-09-16: `feature/pos` sigue en `12fb8045cc264b565cb6e764d95ad7b2447fbfa1`. La presentación aprobada gobierna la adaptación del backend; no se rediseña para ajustar contratos.

## Checkpoint RV10-P7 (2026-09-17, ejecución actual — bloqueo externo)

- SHA inicial confirmado: `6a6dfe3250dbdb338763c3a41aa32b5183877bd3`; referencia fija `12fb8045cc264b565cb6e764d95ad7b2447fbfa1`, árbol `6f61e86a90927dd19d63c557d65376e0b1eaabed`; candidato ejecutable documentado `06f9f6f7741b733bda7770e3b67b714daa59a40b`.
- `git diff --quiet` confirmó que entre el candidato ejecutable y HEAD no cambiaron POS, API, contratos compartidos, manifests raíz ni lockfile. La auditoría de `GUIA_PRUEBA_PO_POS_WINDOWS.md` y `PROMPT_CODEX_PRUEBA_PO_POS_WINDOWS.md` no encontró promesas de disponibilidad API: ambas mantienen fixture visual y recorrido funcional como evidencias separadas, y condicionan API a SHA/health/CORS, terminal activa y credenciales privadas.
- El baseline durable volvió a pasar la verificación de sus 208 PNG y SHA-256; registra Node `22.23.2`, pnpm `10.0.0`, Playwright `1.60.0` y Chromium `148.0.7778.96`. El manifiesto conserva 25 pantallas, 10 secciones de Settings, 11 reportes, 10 vistas de bodega y 208 capturas. Las 4/4 pruebas del verificador Windows y su sintaxis aprobaron.
- El bloqueo externo se repite: Fedora Linux no ofrece Windows, PowerShell, WSL, Wine ni `WSLInterop`; tampoco existen API/terminal/canal autorizados o acceso del PO. No se ejecutaron `check|probe`, Electron/Windows, recorrido API ni una comparación completa nueva del candidato. No se inició servicio, contenedor o BD y los puertos de los ensayos previos permanecen sin listeners.
- Resultado `blocked`: sólo se actualizaron plan, handoff e informe; no cambiaron las guías porque ya reflejan el estado real, ni código, contratos, schemas, migraciones, renderer, CSS, baseline, capturador o comparador. RV10-P7 continúa `[ ]` y no se atribuye disponibilidad o aprobación al PO. Reanudar únicamente en el equipo Windows objetivo con entorno autorizado y registrar por separado inicio/parada, revisión visual y recorrido API sin secretos. Informe: `docs/pos-automation/runs/2026-09-17T20-22-53-001Z-RV10-P7.md`.

## Checkpoint RV10-P7 (2026-09-17, evidencia histórica anterior — guías y evidencia durable)

- SHA inicial/candidato ejecutable: `06f9f6f7741b733bda7770e3b67b714daa59a40b`; referencia fija `12fb8045cc264b565cb6e764d95ad7b2447fbfa1`, árbol `6f61e86a90927dd19d63c557d65376e0b1eaabed`. `db58fda0aca502f6a543bde03e9fd477b3723caa` es ancestro, pero su evidencia visual histórica no se atribuye automáticamente al candidato vigente.
- `GUIA_PRUEBA_PO_POS_WINDOWS.md` distingue revisión visual con fixture y revisión funcional API. Incluye guardas para admitir el commit documental posterior sin aceptar cambios ejecutables, inicio/parada, formato de observaciones, acceso fixture claramente no operativo y el bloqueo explícito de API/Windows/terminal/credenciales.
- `PROMPT_CODEX_PRUEBA_PO_POS_WINDOWS.md` inicia sólo el modo visual por defecto. El modo API exige primero el entorno autorizado y reutiliza `docs/POS_WINDOWS_API_DEMO.md`; pide captura privada mediante `Read-Host`, nunca valores en chat/archivos/evidencia, y prohíbe convertir fixtures o pruebas Linux en persistencia/aceptación.
- Evidencia actual no mutante: el índice versionado apunta a `12fb804`, contiene 208 entradas y los 208 PNG existen con SHA-256 correcto; runtime registrado Node `22.23.2`, pnpm `10.0.0`, Playwright `1.60.0` y Chromium `148.0.7778.96`. `pnpm pos:visual:manifest` confirmó 25 pantallas, 10 secciones de Settings, 11 reportes, 10 vistas de bodega y 208 capturas. Las 4/4 pruebas del verificador Windows y su sintaxis también pasan.
- La comparación 208/208 del 2026-09-09 permanece como resumen histórico versionado de `db58fda`; los directorios candidato/diff vivían en `/tmp` y no se presentan como evidencia recuperable. Después hubo cambios funcionales en el renderer y esta ejecución no capturó/comparó completamente `06f9f6f`.
- Resultado `partial`: el host sigue siendo Linux y no dispone del Windows objetivo, API accesible desde ese equipo, terminal/canal reales ni acceso del PO. No se inició servidor, Electron o BD; no se ejecutó el recorrido funcional ni se atribuyó aprobación. RV10-P7 permanece `[ ]`. Informe: `docs/pos-automation/runs/2026-09-17T20-13-12-758Z-RV10-P7.md`.

## Checkpoint RV10-P4 (2026-09-17, ejecución actual — bloqueo externo)

- SHA inicial confirmado: `2bca5e409c3bdf817f77546a6c69dd7d66af31dc`; referencia fija `12fb8045cc264b565cb6e764d95ad7b2447fbfa1`, árbol `6f61e86a90927dd19d63c557d65376e0b1eaabed`.
- El host continúa en Fedora Linux nativo y no ofrece PowerShell, Windows, WSL, Wine ni `WSLInterop`. Tampoco se proporcionaron la terminal real, un canal privado autorizado o acceso del PO, por lo que no es posible aportar la evidencia de cierre desde Electron/Windows sin ampliar el alcance.
- La preparación local anterior permanece segura y detenida: `.pos-runner/rv10-p4-local/` conserva modo `0700`, sus archivos sensibles modo `0600`, el contenedor `keysar-rv10-p1-rv10-p4` está `exited` con binding configurado sólo a `127.0.0.1:55473`, y no existen listeners en `3005`, `4413`, `55473` o `9223`. No se inició servicio ni se mutó la BD.
- Las pruebas no mutantes actuales aprobaron: 4/4 casos de `verify-pos-windows-demo`, sintaxis Node y manifiesto de referencia con 25 pantallas, 10 secciones, 11 reportes, 10 vistas de bodega y 208 capturas. `git show` reconfirmó el commit/árbol de referencia. No se repitieron tipos, build, integración, Electron o comparación por píxel porque no cambió código ni una superficie visible; sus resultados permanecen sólo como evidencia histórica del checkpoint previo.
- Resultado `blocked`: sólo se actualizaron plan, handoff e informe; no cambiaron código, contratos, API, Prisma, migraciones, renderer, CSS, baseline, capturador o comparador. RV10-P4 permanece `[ ]` y no se atribuye validación al PO. Reanudar en el equipo Windows objetivo siguiendo `docs/POS_WINDOWS_API_DEMO.md`, con SHA/API/terminal autorizados, y registrar inicio, reinicio, login master/operador, sucursal, permisos, parada y ausencia de listeners sin exponer credenciales. Informe: `docs/pos-automation/runs/2026-09-17T20-09-37-222Z-RV10-P4.md`.

## Checkpoint RV10-P4 (2026-09-17, evidencia histórica anterior — preparación local)

- SHA inicial confirmado: `f3d4db30a66dbb75410d2deb1ac3a841e8493a32`; referencia fija `12fb8045cc264b565cb6e764d95ad7b2447fbfa1`, árbol `6f61e86a90927dd19d63c557d65376e0b1eaabed`.
- Se añadió `scripts/verify-pos-windows-demo.mjs check|probe`, sin dependencias ni secretos. Valida Node `22.23.2`, pnpm `10.0.0`, SHA completo/HEAD, `VITE_POS_DATA_MODE=api`, fixture visual apagada, coincidencia `VITE_API_URL`/`POS_API_URL`, terminal presente sólo en el entorno main, `health.release`, `/ready` y preflight CORS. Cuatro pruebas unitarias cubren redacción y rechazos de configuración.
- `docs/POS_WINDOWS_API_DEMO.md` documenta en PowerShell captura privada mediante `Read-Host`, comprobaciones, inicio/reinicio/parada y borrado de variables; `apps/pos/.env.example` ya no parece ofrecer una credencial reutilizable. No se copiaron secretos al renderer, repositorio, comandos documentados o reporte.
- La primera prueba real de Electron encontró un defecto del recorrido: antes de hidratar la sesión API, `activeBranch`/`requestedBranch` están vacíos y el master intentaba cambiar la terminal a esa selección inexistente. La condición en `App.tsx` ahora conserva la sucursal fija devuelta por la terminal en el primer acceso y sólo llama al cambio canónico cuando existe una selección explícita; no cambió JSX, CSS, contratos, schemas, migraciones ni baseline.
- PostgreSQL 16 dedicado aplicó 46/46 migraciones; los provisionadores P2/P3 verificaron dataset, terminal activa, 69/35 permisos, rechazo de código incorrecto y consumo correcto. El nuevo `probe`, tipos y build web-Electron pasaron. Electron `35.7.5` abrió con ventana `1280 × 800`; login master y relogin tras reinicio llegaron a `Conteo inicial de inventario` y mostraron la sucursal sintética fija. El manifiesto de referencia se conserva sin cambio; no se repitió la matriz visual porque no cambió DOM/CSS.
- En el segundo arranque Linux, el repositorio offline conservado no pudo desenvolver su clave `safeStorage`; la app mantuvo el login online exitoso. No se borró caché/outbox para ocultarlo: la prueba offline, Windows y binario instalado siguen en RV8-P6 y no acreditan este MVP online.
- Resultado `partial`: el host es Fedora Linux sin `powershell.exe`, `cmd.exe`, `wsl.exe` o Wine; tampoco existen terminal real/canal privado ni acceso del PO. RV10-P4 permanece `[ ]` hasta ejecutar la guía en Windows y demostrar apertura/login del PO. Informe: `docs/pos-automation/runs/2026-09-17T19-30-40-620Z-RV10-P4.md`.
- Recursos locales preservados y detenidos: contenedor `keysar-rv10-p1-rv10-p4`, BD sintética `keysar_pos_demo` y `.pos-runner/rv10-p4-local/` privado. API, PostgreSQL, Vite, Electron y puertos `3005/4413/55473/9223` quedaron sin listeners.

## Checkpoint RV10-P3 (2026-09-17, evidencia histórica anterior — bloqueo externo)

- SHA inicial confirmado: `1aae14428a90133401726cfe25b1faf45f778042`; referencia fija `12fb8045cc264b565cb6e764d95ad7b2447fbfa1`, árbol `6f61e86a90927dd19d63c557d65376e0b1eaabed`.
- El host continúa en Fedora Linux nativo y no ofrece `powershell.exe`, `cmd.exe`, `wsl.exe` o Wine. Tampoco se proporcionaron una terminal real del PO, un canal privado autorizado ni un development autorizado; por ello no se pueden entregar credenciales ni comprobar el acceso desde Electron/Windows sin ampliar el alcance.
- La preparación local anterior permanece segura y detenida: `prepare-pos-local-demo.sh status` conserva release `c2bc9c46eacf4e414de677275407cdf12d485a9d`, API detenida y PostgreSQL `exited`; `.pos-runner/rv10-p3-local/` conserva modo `0700`, sus tres archivos sensibles `0600`, el contenedor sólo configura `127.0.0.1:55472` y los puertos `4412`/`55472` están libres.
- Se reauditaron sin exponer secretos las guardas y rutas canónicas del helper, la configuración de terminal del proceso principal (`POS_API_URL`, `POS_TERMINAL_CODE`, `POS_TERMINAL_SECRET`) y el IPC `window.electronAPI.posLogin`. `git show` confirmó que REF conserva los campos y rechazos de `Código master`; `pnpm pos:visual:manifest` confirmó 25 pantallas, 10 secciones, 11 reportes, 10 vistas de bodega y 208 capturas.
- Resultado `blocked`: sólo se actualizaron plan, handoff e informe. No cambiaron código, contratos, schemas, migraciones, renderer, CSS, baseline, capturador o comparador; no se inició Electron, no se mutó la BD y no se repitieron tipos, builds, unitarias o integración ya acreditados por el checkpoint parcial anterior.
- RV10-P3 permanece `[ ]`. Reanudar únicamente cuando existan equipo y canal autorizados: registrar una terminal distinta, cargar sus secretos privadamente en el proceso principal y comprobar master/operador, sucursal, permisos y Código master incorrecto/correcto desde Electron/Windows. Informe: `docs/pos-automation/runs/2026-09-17T19-25-59-775Z-RV10-P3.md`.

## Checkpoint RV10-P3 (2026-09-17, evidencia histórica anterior — preparación local)

- Se añadió `backend/api/scripts/provision-pos-terminal-local.ts provision|verify`. El helper consume el dataset sintético de RV10-P2 y las rutas existentes `/api/auth/login`, `/api/pos/terminals`, `/status`, `/api/pos/auth/login`, `/api/pos/authorizations` y `/api/pos/auth/verify`; no duplica hashing, sesiones, permisos, auditoría ni reglas de terminal.
- Es opt-in: exige `POS_TERMINAL_CONFIRMATION=LOCAL_SYNTHETIC_ONLY`, dataset explícito, API `http` en loopback y archivos fuente/salida dentro de `.pos-runner/` con directorios privados. Bloquea redirecciones, API remota, rutas que escapen mediante enlaces simbólicos y archivos con permisos para grupo/otros.
- Registro y secreto son reentrantes. La primera alta conserva inmediatamente el secreto de única lectura en `terminal.json` modo `0600`; una reentrada consulta la terminal por el token POS master y no rota ni imprime el valor. Si una alta previa quedó sin archivo, la recuperación rota mediante la ruta canónica antes de escribir el nuevo secreto.
- Verificación actual: un entorno nuevo `rv10-p3` aplicó 46/46 migraciones y provisionó el dataset base. La terminal adicional quedó `ACTIVE` en la misma sucursal. Master y operador iniciaron sesión con 69/35 permisos; el operador conservó `SALE_CREATE` y no recibió `TERMINALS_MANAGE`. Desde su sesión, `Código master=0000` fue rechazado y el código sintético correcto emitió y consumió una autorización `EMPLOYEES_ACCESS`. `provision`/`verify` pasaron en creación y reentrada.
- Pasaron la integración PostgreSQL POS 10/10, schemas sincronizados/válidos, tipos/lint/136 unitarias/build del API, compilación y lint aislados del helper, tipos/build web-Electron del POS y manifiesto visual 25/10/11/10/208. No cambiaron Prisma, migraciones, contratos, renderer, CSS, baseline, capturador ni comparador; por ello no se repitió la matriz por píxel.
- Resultado `partial`: la evidencia es preparación local sintética, no la terminal real ni aceptación del PO. No hubo Windows/WSL ni canal privado/autorización para entregar credenciales, y no se inició Electron interactivo. RV10-P3 permanece `[ ]`; continuar sólo en el entorno autorizado registrando una terminal distinta y comprobando allí master/operador, sucursal, permisos, Código master incorrecto/correcto y el IPC `window.electronAPI.posLogin`. Informe: `docs/pos-automation/runs/2026-09-17T19-12-25-154Z-RV10-P3.md`.
- Recursos a preservar al relevo: contenedor detenido `keysar-rv10-p1-rv10-p3`, BD `keysar_pos_demo` sintética y estado ignorado `.pos-runner/rv10-p3-local/` con `config.env`, `credentials.json` y `terminal.json` privados. No reutilizar con otro SHA/dataset ni copiar sus valores a documentación, renderer o consola.

## Checkpoint RV10-P2 (2026-09-17, evidencia histórica anterior)

- Se añadió `backend/api/scripts/provision-pos-local-demo.ts` con acciones `provision` y `verify`. Es opt-in: exige `POS_DEMO_CONFIRMATION=LOCAL_SYNTHETIC_ONLY`, `POS_DEMO_DATASET_ID`, `DATABASE_URL`/`DIRECT_URL` coincidentes en loopback con base `keysar_pos_demo[_sufijo]`, usuario no superadministrador y `POS_DEMO_CREDENTIALS_FILE` dentro de `.pos-runner/`.
- Antes de cargar datos, consulta la conexión efectiva y rechaza cualquier base con datos operativos previos que no tenga el marcador auditable del dataset. Todo el alta ocurre en una transacción. La repetición conserva historiales append-only, no duplica filas y vuelve a verificar el conjunto completo.
- Los secretos se generan localmente, se guardan sólo en JSON ignorado con modo `0600` y nunca se imprimen. El helper reutiliza `hashPosSecret`, `fingerprintSecret`, el árbol de permisos migrado, `pos-loc-matrix` y los bancos/redes/MSI canónicos. No usa `.env`, el seed general ni una BD compartida.
- El dataset `rv10-p2` contiene una sucursal/perfil, puestos/empleados/usuarios master y operador, 35 permisos operativos sin privilegios administrativos, una terminal exclusivamente sintética local, cuatro artículos publicados con precios, saldos de sucursal/bodega, dos clientas, tres métodos/políticas, membresía de cuatro sesiones, dos cortesías y configuración Scheduler interna con 12 reglas semanales. Mensajería queda deshabilitada y todo se etiqueta como demo/sintético.
- Verificación actual: PostgreSQL 16 aplicó 46/46 migraciones; `provision` pasó en creación y reentrada; `verify` pasó; el API local aceptó login master/operador y devolvió 69/35 permisos, cuatro artículos, tres métodos y 220 slots. Las guardas rechazaron invocaciones sin confirmación, host remoto, nombre de BD no dedicado y otra BD local migrada con un dato previo no marcado. Pasaron schemas, tipos/lint/136 unitarias/build del API, compilación aislada del helper, tipos/build web-Electron del POS y manifiesto visual 25/10/11/10/208.
- No cambiaron Prisma, migraciones, contratos compartidos, renderer, CSS, baseline, capturador ni comparador. No se repitió la matriz visual porque no cambió una superficie visible. La terminal creada sólo prueba la validez local de los actores y no es la terminal del PO de RV10-P3.
- Resultado `completed`: RV10-P2 queda cerrado sin pendientes dentro de su criterio. RV10-P1 sigue abierto por su evidencia externa; RV10-P3 debe crear/activar la terminal del PO y entregar sus credenciales por canal privado únicamente en el entorno autorizado. Informe: `docs/pos-automation/runs/2026-09-17T18-46-25-737Z-RV10-P2.md`.
- Recursos preservados y detenidos al cierre: contenedor local `keysar-rv10-p1-rv10-p2`, bases sintéticas `keysar_pos_demo` y `keysar_pos_demo_guard`, y estado ignorado `.pos-runner/rv10-p2-local/`. La base `guard` conserva sólo el dato sintético no marcado usado para acreditar el rechazo. No eliminar ni reutilizar con otro SHA/dataset; para repetir, usar recursos dedicados nuevos o los mismos valores exactos.

## Checkpoint RV10-P1 (2026-09-17, evidencia histórica anterior — bloqueo externo)

- Reanudación bloqueada sobre el SHA inicial `f7fc12037fe0c8815b78315cf8ac9d9f86c57c44`; referencia de presentación/interacción y manifiestos confirmados en `12fb8045cc264b565cb6e764d95ad7b2447fbfa1`. No cambiaron renderer, CSS, baseline, capturador, comparador, API, schema, migraciones ni helper.
- El entorno es Fedora Linux nativo y no contiene `powershell.exe`, `cmd.exe` o `wsl.exe`; tampoco existe un environment development autorizado en el alcance. Por ello no es posible aportar la evidencia pendiente desde Windows ni ampliar de forma segura el listener más allá de loopback.
- Se revalidó sin mutar datos el recurso del checkpoint anterior: `prepare-pos-local-demo.sh status` informa release `27b92cacc195f48020033940166a9f5f6460522b`, API detenida y PostgreSQL detenido; el contenedor conserva `127.0.0.1:55470`, los archivos locales conservan modo `0600`, los puertos `4410`/`55470` están libres y la sintaxis Bash pasa.
- Resultado `blocked`: sólo se actualizaron plan, handoff e informe. No se repitieron tipos, builds, unitarias, integración HTTP/BD o matriz visual porque no cambió código ni una superficie visible; sus resultados pertenecen al checkpoint parcial anterior y no se presentan como ejecución actual.
- Siguiente acción precisa: desde Windows/WSL autorizado o un development aislado autorizado, preparar el SHA candidato en un estado/contenedor dedicado, verificar la URL desde el entorno consumidor y registrar `health.release`, `/ready` y el preflight CORS. No usar el estado local anterior con otro SHA ni abrir `0.0.0.0` sin definir antes una interfaz/red privada confiable y sus controles. Informe: `docs/pos-automation/runs/2026-09-17T18-42-54-135Z-RV10-P1.md`.

### Evidencia histórica del checkpoint parcial anterior

- SHA inicial/candidato verificado: `27b92cacc195f48020033940166a9f5f6460522b`; referencia de presentación e interacción: árbol completo `12fb8045cc264b565cb6e764d95ad7b2447fbfa1`. No cambió renderer, CSS, baseline, capturador ni comparador.
- Se añadió `backend/api/scripts/prepare-pos-local-demo.sh`, helper POS/API opt-in con acciones `up`, `verify`, `status` y `down`. Sólo acepta API y PostgreSQL en loopback, exige `POS_DEMO_CONFIRMATION=LOCAL_SYNTHETIC_ONLY`, un contenedor con prefijo dedicado y un estado dentro de `.pos-runner/`; no lee `.env`, no descarga imágenes, no ejecuta seeds y no elimina la BD al detenerse.
- El helper genera localmente contraseña y secretos de API con permisos `0600`, fija `RELEASE_SHA` al commit candidato, usa Scheduler interno y mensajería deshabilitada, aplica `prisma migrate deploy`, compila el API y verifica `health.release`, `/ready`, CORS permitido/rechazado, listeners y ledger de migraciones. `backend/api/src/index.ts` admite `HOST`; el default productivo sigue siendo `0.0.0.0`, mientras el helper fija `127.0.0.1`.
- Verificación actual: dos ejecuciones `up` (creación y reentrada) pasaron con PostgreSQL 16, 46/46 migraciones, SHA exacto, conexión y CORS. `app.integration.test.ts` pasó 5/5 sobre la BD dedicada; schemas sincronizados/válidos, type-check/lint/136 unitarias del API y type-check/build web-Electron del POS también pasaron. La prueba negativa confirmó que `up` falla sin la confirmación literal.
- Resultado `partial`: esta máquina no ofrece Windows ni un entorno development autorizado. Por ello no se expuso ningún puerto a la red y no se verificó la URL desde el equipo objetivo; esa comprobación pertenece todavía al criterio de RV10-P1. RV10-P2 no empezó: no se cargaron datos demo, credenciales ni terminales.
- Recurso local conservado y detenido: contenedor `keysar-rv10-p1-pg`, ligado durante las pruebas sólo a `127.0.0.1:55470`, con la BD sintética `keysar_pos_demo`; API detenida y puerto `4410` libre. Estado/secretos locales ignorados en `.pos-runner/rv10-p1-local/`, sin valores documentados. Para repetir localmente: `POS_DEMO_CONFIRMATION=LOCAL_SYNTHETIC_ONLY POS_DEMO_EXPECTED_SHA=27b92cacc195f48020033940166a9f5f6460522b backend/api/scripts/prepare-pos-local-demo.sh up`; para detener preservando datos: `backend/api/scripts/prepare-pos-local-demo.sh down`.
- Siguiente acción precisa: ejecutar ese mismo candidato en Windows/WSL autorizado y comprobar desde Windows `health.release`, `/ready` y el preflight CORS, o autorizar un development con aislamiento acordado. No abrir el listener fuera de loopback sin definir antes la interfaz/red confiable y el control de acceso. Informe: `docs/pos-automation/runs/2026-09-17T18-28-31-657Z-RV10-P1.md`.

## Checkpoint RV9-P4 (2026-09-16, ejecución actual)

- SHA inicial verificado: `4bf4dafea066ac580c7b91439509e84088c47240`; referencia de presentación/interacción: árbol completo `12fb8045cc264b565cb6e764d95ad7b2447fbfa1` y manifiesto de 208 capturas.
- Se reauditaron R01–R17 contra renderer, contratos, rutas/servicios, Prisma, Electron/offline y consumidores Envelope/Payroll/Scheduler. La clasificación y evidencia retiro por retiro se añadió a `docs/POS_UI_BACKEND_COMPATIBILITY.md` sin reescribir la matriz histórica de RV1.
- Limpieza ejecutada: `EmployeesView` limita el código personal de una alta nueva a cuatro dígitos tanto mediante `maxLength` como al normalizar el evento. Esto coincide con REF, el placeholder/validación ya presentes y `posEmployeeWriteSchema`. No cambia la lectura de credenciales históricas 4–12 documentada en `posLoginRequestSchema`, `posMasterAuthorizationRequestSchema` y `posAttendanceIdentifySchema`.
- Se conservaron deliberadamente: modo mock/fixtures y cola local condicionados a inventario/RV8; gateway mock y provider/tablas Agenda por evidencia/rollback; `Venta`/`VentaDetalle`/`PosLegacySaleProjection` por Envelope y conciliación; My Account, Websites y copy mock por B01–B03. No se tocaron datos, migraciones, schemas, API, servicios externos, baseline, capturador ni comparador.
- Verificaciones de esta ejecución: type-check y build web/Electron del POS, contrato unitario dirigido del API, manifiesto visual, búsquedas de residuos/consumidores, comparación directa con `git show`, formato y `git diff --check`. No se ejecutó integración PostgreSQL porque no cambió backend, contrato, schema, migración ni persistencia; no se repitió la matriz visual porque el único cambio visible es una restricción de entrada que restaura el atributo de REF sin alterar geometría.
- Resultado: `completed`. RV9-P4 queda cerrado como auditoría y limpieza local segura, no como retiro de los elementos condicionados. Informe: `docs/pos-automation/runs/2026-09-16T22-51-26-680Z-RV9-P4.md`.

## Checkpoint RV5-P1 (2026-09-16, ejecución actual — bloqueo documentado)

- SHA inicial verificado: `ab2d49eeeb739025c6dd1f45302c2f5c747e200b`; referencia visual reauditada: árbol completo `12fb8045cc264b565cb6e764d95ad7b2447fbfa1`.
- `TicketEditDialog` ofrece únicamente `CLIENTE Y VENTA`, `DIVISIÓN DE VENTA`, `PRODUCTOS Y SERVICIOS` y `COBRO Y APARTADO`. La empresa se muestra con candado como participación obligatoria. No hay selector de tarjetón/unidad, paquete/cortesía, cita, servicio, horario ni slot de Agenda que identifique las operaciones restantes.
- El backend ya conserva la última proyección y bloquea de forma explícita las ambigüedades: `planTicketMembershipRevision` rechaza altas/bajas/sustituciones sin unidad; `appendTicketRevision` preserva paquetes/cortesías/empresa y rechaza cambios de identidad o composición; las citas externas exigen recuperación durable; dos adeudos ligados a la misma línea no se asignan por heurística. Esos rechazos ocurren antes de consumir la autorización y tienen evidencia HTTP/BD histórica en los checkpoints anteriores.
- Decisión de esta ejecución: no inferir qué tarjetón cancelar, cómo recomponer un paquete/cortesía, qué identidad comercial sustituir ni cómo reprogramar Agenda. No se cambió código, schema, migración, contrato, renderer, fixture, baseline, capturador ni comparador; `CLAUDE.md` tampoco cambia porque no apareció un contrato, módulo, comando o arquitectura nuevos.
- Verificaciones de esta ejecución: inspección dirigida del árbol aprobado con `git show`, localización de guardas/pruebas con `rg`, Prettier de los tres documentos modificados y `git diff --check`. No se ejecutaron tipos, builds, unitarias, integración PostgreSQL ni matriz visual porque el resultado `blocked` sólo modifica documentación y no altera superficies ejecutables. La evidencia funcional de 46 migraciones, 136 unitarias, 13 casos integrados y gate de carga pertenece al checkpoint 8, no a esta ejecución.
- Resultado `blocked`: RV5-P1 permanece `[ ]`. Para reanudar código, Producto debe aprobar un control o regla inequívoca de identidad/composición y, para Agenda HTTP, el mecanismo de recuperación durable. B01–B03, Windows/hardware, RV8 y aceptación del PO siguen diferidos. Informe: `docs/pos-automation/runs/2026-09-16T22-46-34-049Z-RV5-P1.md`.

## Checkpoint RV5-P1 (2026-09-16, evidencia histórica anterior — segundas revisiones y reportes efectivos)

- SHA inicial verificado: `1f9e9dd8de63cb5989eb4ba0a405ad260ecf0b45`; referencia visual intacta: árbol completo `12fb8045cc264b565cb6e764d95ad7b2447fbfa1`.
- `appliedTicketRevisionSnapshot` es ahora el lector compartido de la proyección efectiva. Cada evento nuevo conserva además familia/categoría por artículo junto con costos e identidades; los eventos anteriores siguen siendo legibles porque el campo es opcional y cae a los snapshots originales.
- `SALES_DETAIL` usa líneas, vendedores y sólo los IDs de cobro efectivos de la última revisión. `SOLD_PRODUCTS`/`MERCHANDISE_PROFITABILITY` usan líneas, importes, costos y metadatos efectivos. `SALES_BY_EMPLOYEE`, `EMPLOYEE_PERFORMANCE` y `EMPLOYEE_DAILY` usan el reparto vigente. Reporte y exportación comparten exactamente el dataset; conciliación bancaria conserva todos los movimientos compensatorios con signo. No se reescribieron `PosTicketLine`, `PosTicketSeller`, cobros, eventos ni proyecciones legacy.
- Una venta de dos unidades por `100.00` se revisó a tres por `120.00` y después a cuatro por `160.00`. La segunda llave se reenvió sin duplicar: quedaron dos eventos (versiones 2/3), operaciones `100/-100/+120/-120/+160`, neto `160.00`, inventario `6.00` y sólo el cobro `REVISION 160.00` como pago visible. Detalle, productos, ventas por empleado, sus tres exportaciones y conciliación/exportación bancaria coincidieron; cuatro exports dejaron auditoría.
- PostgreSQL 16 dedicado aplicó 46/46 migraciones y pasó 10/10 casos POS más 3/3 POS/Scheduler. El gate opt-in de reportes pasó 2/2 sobre 1/10/20/30 sucursales, 12,000 tickets y 24,000 líneas; a 30 sucursales el reporte paginado completo midió `7,226.92 ms`, hidratación `6,626.97 ms`, exportación `2,433.30 ms` y máximo de página `1,097.22 ms`, bajo sus umbrales técnicos. También pasaron schemas, tipos, lint, 136/136 unitarias, builds API/POS y manifiesto de 208 capturas.
- Resultado `partial`: se cerró la certificación transversal independiente. Permanecen bloqueados los cambios que el diálogo aprobado no identifica y Agenda HTTP/reprogramación sin saga durable; B01–B03, Windows/hardware y aceptación del PO no cambiaron. El contenedor local `keysar-rv5-p1-pg-20260916-reports`, ligado sólo a `127.0.0.1:55467`, conserva datos sintéticos y queda detenido. El secreto se generó localmente y no se imprimió/versionó; no se leyó `.env`, no se ejecutó seed general ni se tocó una BD compartida. Informe: `docs/pos-automation/runs/2026-09-16T22-31-16-196Z-RV5-P1.md`.

## Checkpoint RV5-P1 (2026-09-16, evidencia histórica anterior — adeudos repetidos identificados por línea)

- SHA inicial verificado: `b892d9b5dab0493f047e35277012ec0e8fcaffb0`; referencia visual intacta: árbol completo `12fb8045cc264b565cb6e764d95ad7b2447fbfa1`.
- Cada fila existente de `TicketEditDialog` ya recibe `Ticket.products[].backendLineId`. El diálogo ahora lo conserva como `ticketLineId` opcional al guardar y `App.tsx` lo transmite en `PosTicketRevisionRequestDto`; no se añadió campo, copy, botón, estado visible ni cambio de geometría. Las filas nuevas continúan sin ID hasta persistirse.
- `appendTicketRevision` rechaza identidades repetidas, ajenas o cruzadas antes de consumir autorización. Conserva el ID de la línea en la proyección y, cuando un artículo tiene varios adeudos, exige una correspondencia uno a uno entre `PosOwedProduct.ticketLineId`, línea vigente y línea revisada. Dos adeudos que comparten el mismo `ticketLineId` siguen siendo ambiguos y permanecen bloqueados.
- La integración creó el mismo producto como línea suelta y como línea de un paquete publicado. Ambos adeudos conservaron IDs distintos y entregas parciales de `1.00/2.00`. La revisión aumentó únicamente la fila suelta de `2.00` a `4.00`, conservó la fila del paquete en `3.00`, mantuvo pendientes `3.00/1.00`, existencia `7.00`, un evento y operaciones `SALE/REFUND/REVISION` por `500.00/500.00/700.00`; la proyección legacy neta fue `700.00`. El replay fue idéntico.
- Una solicitud con IDs cruzados obtuvo `409` y dejó reutilizable el token. Después del caso válido, una fila sintética adicional que compartía el mismo `ticketLineId` acreditó el bloqueo del caso realmente ambiguo y tampoco consumió su autorización. No se relajó la prohibición de dos líneas idénticas de checkout.
- PostgreSQL 16 desechable reconstruyó 46/46 migraciones y la verificación final pasó 10/10 casos POS y 3/3 POS/Scheduler. También pasaron schemas sincronizados/válidos, contratos, tipos de types/API client/API/POS, lint, 136/136 unitarias, builds API/POS y manifiesto de 208 capturas. No se repitió captura/comparación: los únicos cambios del renderer transportan identidad interna y no alteran JSX visible, CSS, fixtures, baseline, capturador ni comparador.
- Resultado `partial`: quedan altas/bajas o sustituciones sin identidad inequívoca, composición variable, reprogramación/Agenda externa y certificación transversal restante. B01–B03, Windows/hardware y aceptación del PO no cambiaron. Recursos locales conservados y detenidos al cierre: cinco contenedores PostgreSQL de diagnóstico/verificación ligados durante pruebas sólo a `127.0.0.1:55462–55466`, con datos sintéticos; el intento de `55462` usó un fixture inválido de líneas exactamente duplicadas y el de `55463` demostró que paquete+línea ya aporta orden canónico antes de ajustar la aserción. Los secretos se generaron localmente y no se imprimieron/versionaron; no se leyó `.env`, no se ejecutó seed general ni se tocó una BD compartida. Informe: `docs/pos-automation/runs/2026-09-16T22-10-12-534Z-RV5-P1.md`.

## Checkpoint RV5-P1 (2026-09-16, evidencia histórica anterior — participación COMPANY preservada)

- SHA inicial verificado: `30ccbcc531cdedee5bc6028335aa2320e97199d1`; referencia visual intacta: árbol completo `12fb8045cc264b565cb6e764d95ad7b2447fbfa1`.
- El diálogo aprobado ya conserva la empresa como participante bloqueado y envía su `companyId` junto con los vendedores. `appendTicketRevision` usa esa identidad: compara ID/código/nombre con `PosTicketParticipant`, bloquea `PosCommercialCompany` y rechaza alta, retiro o sustitución con `409` antes de consumir autorización. No se agregó campo, copy, contrato, renderer, schema ni migración.
- El reparto igualitario se recalcula entre todos los participantes. `PosTicketEvent.snapshot.after.participants` conserva la identidad comercial y `effects.companyParticipantPreservations` registra antes/después; la fila original `PosTicketParticipant` permanece inmutable. Las compensaciones y nuevas proyecciones reutilizan `sellerProjectionPayments`, por lo que `Venta`/`PosLegacySaleProjection` reciben sólo la fracción de vendedores y nunca convierten la parte de empresa en comisión humana.
- PostgreSQL 16 desechable reconstruyó 46/46 migraciones y la verificación final pasó 10/10 casos POS y 3/3 POS/Scheduler. El caso empresarial creó una venta `100/100`, rechazó otra empresa con el token intacto, revisó a `110/110` y repitió la misma llave sin duplicar. Los cobros completos quedaron `SALE/REFUND/REVISION` por `200.00/200.00/220.00`; las proyecciones humanas quedaron `100.00/-100.00/110.00`, neto `110.00`, mientras la fila comercial persistida conservó `100.00` y la lectura efectiva mostró `110.00` desde el evento.
- También pasaron schemas sincronizados, tipos/lint/build API, 136/136 unitarias, tipos/build web/Electron POS y manifiesto de 208 capturas. No se repitió captura/comparación: no cambiaron renderer, markup, CSS, fixtures, baseline, capturador ni comparador.
- Resultado `partial`: quedan composición variable y paquetes retirados, reprogramación/Agenda externa, altas/bajas de membresía, adeudos duplicados ambiguos y certificación transversal restante. B01–B03, Windows/hardware y aceptación del PO no cambiaron. Recurso local conservado y detenido al cierre: `keysar-rv5-p1-pg-20260916-company`, ligado sólo a `127.0.0.1:55461` durante pruebas, con las bases sintéticas `keysar_rv5_p1_company` y `keysar_rv5_p1_company_final`. El secreto se generó localmente y no se imprimió/versionó; no se leyó `.env`, no se ejecutó seed general ni se tocó una BD compartida. Informe: `docs/pos-automation/runs/2026-09-16T21-54-29-655Z-RV5-P1.md`.

## Checkpoint RV5-P1 (2026-09-16, evidencia histórica anterior — paquetes y cortesías preservados)

- SHA inicial verificado: `68e1ea826f306a10975beb1feef1f031725ee150`; referencia visual intacta: árbol completo `12fb8045cc264b565cb6e764d95ad7b2447fbfa1`.
- `appendTicketRevision` reconstruye el `packageId` desde las líneas efectivas sólo cuando orden, artículo, cantidad y precio coinciden exactamente con el ticket. Bloquea la fila `PosPackage`, vuelve a usar la cotización/compensaciones existentes y conserva ID, nombre, versión y líneas en `before`, `after` y `effects.packagePreservations`. Alta, baja, reorden o sustitución conserva `409` antes del token; un paquete no publicado o fuera de vigencia no se reinterpreta.
- Las filas `PosCourtesy` quedan bloqueadas y deben corresponder uno a uno con una línea `GIFT` original y, cuando aplica, con la cita del mismo ticket. La línea regalo se preserva con su ID/snapshots y se excluye de cotización e inventario; política, autorización, producto, paquete y cita quedan en `courtesyPreservations` sin mutar `PosCourtesy`, `PosAppointment`, `AgendaReservation` o `SchedulerAppointment`.
- PostgreSQL 16 desechable reconstruyó 46/46 migraciones y la verificación final pasó 10/10 casos POS y 3/3 POS/Scheduler. El caso paquete rechazó cambiar `1×150.00` a `2×75.00` sin consumir la autorización, aplicó después una corrección de clienta conservando la línea/paquete, y el replay mantuvo un solo evento. El caso cortesía rechazó retirar `GIFT`, reutilizó el token y revisó `100.00 → 110.00` conservando byte-a-byte cortesía, cita, reservación, Scheduler y capacidad.
- También pasaron schemas sincronizados, tipos/lint/build API, 136/136 unitarias, tipos/build web/Electron POS, manifiesto visual de 208 capturas, Prettier y `git diff --check`. No se repitió captura/comparación: no cambiaron renderer, markup, CSS, fixtures, baseline, capturador ni comparador.
- Resultado `partial`: quedan altas/bajas/reorden/sustitución de paquete o cortesía, paquetes retirados, participantes `COMPANY`, reprogramación/Agenda externa, altas/bajas de membresía, adeudos duplicados ambiguos y certificación transversal restante. B01–B03, Windows/hardware y aceptación del PO no cambiaron.
- Una repetición diagnóstica contra la primera BD ya poblada chocó con la unicidad de `PosCredential.pinFingerprint` del fixture y dejó 10 fallas en cascada; Scheduler pasó 3/3. No se cambió producción para tolerar una precondición inválida: las corridas finales se hicieron sobre bases vacías y aprobaron 13/13, incluida la conciliación financiera explícita. Recurso local conservado y detenido al cierre: `keysar-rv5-p1-pg-20260916-packages-courtesies`, ligado sólo a `127.0.0.1:55460` durante pruebas, con tres bases sintéticas. El secreto se generó localmente y no se imprimió/versionó; no se leyó `.env`, no se ejecutó seed general ni se tocó una BD compartida. Informe: `docs/pos-automation/runs/2026-09-16T21-32-27-443Z-RV5-P1.md`.

## Checkpoint RV5-P1 (2026-09-16, evidencia histórica anterior — citas Scheduler preservadas)

- SHA inicial verificado: `7dff7d14425e07707247cbd421e3f52491300ab9`; referencia visual intacta: árbol completo `12fb8045cc264b565cb6e764d95ad7b2447fbfa1`.
- `appendTicketRevision` dejó de rechazar todas las citas. El subconjunto permitido no edita Agenda: bloquea `PosAppointment` y su `SchedulerAppointment`, vuelve a leerlos y valida clienta canónica, sucursal, servicio, horario, reservación/slot, membresía, estado y unidades de capacidad antes de consumir la autorización.
- `PosTicketEvent.snapshot.before/after` y `effects.appointmentPreservations` conservan IDs, servicio, intervalos, capacidad, profesionales, recursos y beneficio de membresía. `PosAppointment`, `AgendaReservation`, `SchedulerAppointment`, sus servicios/asignaciones y eventos de sincronización quedan byte-a-byte sin cambios; la revisión reutiliza las compensaciones financieras existentes.
- Una cita no enlazada a Scheduler o inconsistente responde `409` antes del token. El diálogo aprobado no contiene reprogramación ni sustitución de cita, por lo que proveedor HTTP, cambio de slot/servicio y recuperación externa siguen bloqueados; no se agregó ningún control, campo, renderer, contrato compartido o migración.
- PostgreSQL 16 desechable reconstruyó 46/46 migraciones. `pos.integration.test.ts` pasó 10/10 y `pos-scheduler.integration.test.ts` 3/3: una corrección `100.00 → 110.00` llegó a versión 2 sin cambiar cita/reservación/autoridad Scheduler, sin crear cita ni evento adicional, y registró la capacidad preservada; el caso no enlazado dejó la autorización intacta.
- También pasaron schemas sincronizados, tipos/lint/build API, 136/136 unitarias, tipos/build web/Electron POS, manifiesto visual de 208 capturas, Prettier y `git diff --check`. No se repitió captura/comparación: no cambiaron renderer, markup, CSS, fixtures, baseline, capturador ni comparador.
- Resultado `partial`: quedan reprogramación/Agenda externa, altas/bajas/sustituciones de tarjetones, paquetes/cortesías, participantes `COMPANY`, adeudos duplicados ambiguos y certificación transversal restante. B01–B03, Windows/hardware y aceptación del PO no cambiaron.
- Recursos locales conservados y detenidos: `keysar-rv5-p1-pg-20260916-agenda`, `...-agenda-final`, `...-agenda-final2`, `...-agenda-verify`, `...-agenda-final3` y `...-agenda-final4`, ligados sólo a `127.0.0.1:55454–55459` durante pruebas y con datos sintéticos. Una corrida diagnóstica falló sólo porque el fixture nuevo omitió el snapshot Agenda exigido por constraint; se corrigió el fixture sin debilitar producción. Otro intento encontró la transición normal del servidor temporal de inicialización de PostgreSQL antes de migrar; la guarda de disponibilidad se estabilizó y la confirmación final en una base nueva aprobó. Los secretos se generaron localmente y no se imprimieron/versionaron; no se leyó `.env`, no se ejecutó seed general ni se tocó una BD compartida. Informe: `docs/pos-automation/runs/2026-09-16T21-13-03-305Z-RV5-P1.md`.

## Checkpoint RV9-P3 (2026-09-16, evidencia histórica anterior)

- SHA inicial verificado: `c1fbb602b5f4dcb37f6d33f6d6f1bc1fa5bc0f27`; referencia visual intacta: árbol completo `12fb8045cc264b565cb6e764d95ad7b2447fbfa1`.
- Se añadieron `scripts/verify-pos-migration-recovery.sh` y `backend/api/scripts/pos-migration-recovery-fixture.ts`. El gate es opt-in, exige tres bases distintas, vacías, en loopback, con `pos_upgrade` en su nombre, schema `public` y confirmación `EPHEMERAL_ONLY`; nunca crea, vacía o elimina bases. Usa clientes PostgreSQL locales o un contenedor Podman explícito.
- El snapshot representativo termina en `20260909010000_pos_rv4_catalog_inventory_settings` (45 migraciones). Contiene sólo datos sintéticos: sucursal, operadora, terminal, clienta, ticket/membresía por `450.00`, pago, auditoría y `PosSyncOperation` pendiente. Hashes y secretos del fixture se generan en cada corrida y no se imprimen.
- El respaldo lógico custom midió `825751` bytes y quedó identificado por `SHA-256 5c03cacb3187c5bf7fdeb0de3b3b799e00c1f6802da835af9eb3c664dbc75b93`. Se validó que incluyera `_prisma_migrations`, `PosTicket`, `PosClientMembership`, `PosSyncOperation` y `AuditLog`; se restauró en dos bases limpias. Ambas conservaron las 45 migraciones, recibieron `20260916010000_add_pos_membership_revision_projections` y terminaron en 46/46.
- Después de cada upgrade, `ticketDto` recuperó total `450.00`, un pago y una membresía; `membershipDto` recuperó importe `450.00` y tres sesiones restantes. La operación offline continuó `PENDING`, la auditoría persistió y existieron tanto la tabla de proyección como su trigger append-only. Una cuarta base nueva pasó 46/46 y 10/10 pruebas de `pos.integration.test.ts`.
- También pasaron guardas negativas de destino remoto y base no vacía, schemas sincronizados/válidos, revisión de migraciones, tipos/lint/build API, 136/136 unitarias, tipos/build web/Electron POS y el manifiesto intacto de 208 capturas. No cambió producción, Prisma, migraciones, contratos, renderer, CSS, baseline, capturador ni comparador; no se repitió la comparación visual porque no cambió ninguna superficie visible.
- La primera ejecución diagnóstica se detuvo después de crear el snapshot, antes del respaldo, porque `node --import tsx` resolvía desde la raíz sin esa dependencia directa. Se cambió al binario workspace `pnpm --dir backend/api exec tsx` y la prueba completa se repitió desde tres bases nuevas. No hubo datos compartidos ni limpieza destructiva.
- Recurso local a conservar detenido al cierre: `keysar-rv9-p3-pg-20260916-202747`, ligado durante pruebas sólo a `127.0.0.1:55455`. Conserva las bases sintéticas del primer diagnóstico, las dos restauraciones migradas y la base de integración; el respaldo temporal se descartó después de validar ambas restauraciones, pero su tamaño/hash y el procedimiento reproducible quedan documentados. El secreto fue generado localmente, no se imprimió/versionó; no se leyó `.env`, no se ejecutó seed general ni se tocó una BD compartida. Informe: `docs/pos-automation/runs/2026-09-16T20-27-47-323Z-RV9-P3.md`.

## Checkpoint RV7-P2 (2026-09-16, evidencia histórica anterior)

- SHA inicial verificado: `7c1f69a4310acd55dd25185e813ccbfdf92eefc7`; referencia: árbol completo `12fb8045cc264b565cb6e764d95ad7b2447fbfa1`.
- Se añadió sólo el gate opt-in `backend/api/src/pos-report-load.integration.test.ts`. Reutiliza sin refactor de producción `/api/pos/reports/:key`, `/api/pos/exports/:key`, alcance histórico canónico, permisos `REPORTS_VIEW`/`REPORTS_PRINT`/`REPORTS_COSTS` y `AuditLog`. No cambió renderer, contratos, API client, Prisma, migraciones, baseline, manifiestos, capturador ni comparador.
- Antes de la corrida válida se fijaron umbrales técnicos locales configurables: `2,000 ms` por página, `120,000 ms` para el recorrido paginado completo y `30,000 ms` por exportación. El fixture usa 400 tickets por sucursal, dos líneas por ticket, 366 días, 30 sucursales con tres históricas inactivas, nombres largos, vacíos y nulos; no representa un SLA ni aceptación operativa.
- Resultado de `SALES_DETAIL` para 1/10/20/30 sucursales: 400/4,000/8,000/12,000 filas; reporte paginado completo `208.86/1,791.63/4,105.43/7,097.17 ms`; hidratación de tickets de la pantalla `186.75/1,749.01/3,962.97/6,640.67 ms`; archivo `92.48/769.27/2,611.71/2,401.79 ms`. El conjunto real de folios de pantalla, el reporte y el archivo conciliaron y conservaron 0/1/2/3 sucursales inactivas.
- Los 13 `POS_REPORT_KEYS` conciliaron total y primera página contra el archivo completo en 30 sucursales. Los mayores conjuntos fueron 24,000 filas en `SOLD_PRODUCTS` y `MERCHANDISE_PROFITABILITY`; sus exportaciones tardaron `1,896.33 ms` y `2,090.81 ms`. El máximo de página fue `1,144.16 ms` en `BANK_RECONCILIATION`, bajo el gate. Un actor sin costos no recibió columnas protegidas; master sí recibió `Costo`/`Utilidad`; los valores largos/vacíos y 400 filas por sucursal inactiva llegaron intactos, y la auditoría registró sucursales/filas.
- Validación adicional: 46/46 migraciones en bases nuevas; suite integral 21/21 con los dos casos de carga omitidos por defecto; lint y 136/136 unitarias; tipos/build API; tipos/build web/Electron POS; manifiesto 25/10/11/10 y 208 capturas; Prettier y `git diff --check`. Las tres fallas de preparación previas fueron del fixture nuevo (longitud de ubicación, normalización del código terminal y extremo inclusivo de 366 días); se corrigieron sin tocar producción y cada diagnóstico quedó conservado en una base separada.
- Recurso local a conservar detenido al cierre: `keysar-rv7-p2-pg-20260916-200055`, ligado durante pruebas sólo a `127.0.0.1:55454`, con las bases sintéticas `keysar_rv7_p2`, `keysar_rv7_p2_final`, `keysar_rv7_p2_final2`, `keysar_rv7_p2_final3`, `keysar_rv7_p2_final4`, `keysar_rv7_p2_final5` y `keysar_rv7_p2_regression`. El secreto fue generado localmente y no se imprimió/versionó; no se leyó `.env`, no se ejecutó seed general ni se tocó una BD compartida. No se solicitó ni atribuyó aprobación al PO. Informe: `docs/pos-automation/runs/2026-09-16T20-00-55-973Z-RV7-P2.md`.

## Checkpoint RV6-P2 (2026-09-16, evidencia histórica anterior)

- SHA inicial verificado: `b54288c902f914c3a2c95f130fc7ecb3845ab34f`; referencia: árbol completo `12fb8045cc264b565cb6e764d95ad7b2447fbfa1`.
- `backend/api/src/pos-scheduler.integration.test.ts` prepara únicamente datos sintéticos y valida disponibilidad, capacidad dos, clienta nueva/existente, cortesía simple/doble simultánea, próxima sesión idempotente, cancelación, asistencia y no-show. La integración Scheduler existente usa viernes futuros y vuelve a comprobar replay/conflicto optimista y carrera por el último profesional.
- `confirmPreparedInternalAgenda` comprueba que el servicio del slot coincide con el solicitado. `reserveMembershipNextSession` deriva del slot interno el `SchedulerServiceProfile` y artículo `SERVICE` reales, por lo que ya no guarda el artículo `MEMBERSHIP` como servicio de cita. Un `NO_SHOW` libera el beneficio reservado; sólo `ATTENDED` lo consume e incrementa el saldo usado una vez.
- La matriz dirigida pasó 5/5 sobre una base fresca. La suite integral pasó 21/21 en cinco archivos; `scheduler-load.integration.test.ts` quedó omitida por su guarda explícita. También pasaron 46/46 migraciones, schemas sincronizados/válidos, lint, 136/136 unitarias, tipos/build de API y POS y manifiesto visual de 208 capturas.
- La comparación por fuente con `12fb804` confirma los estados visibles `AGENDADA`, `ASISTIÓ · SESIÓN APLICADA`, `NO LLEGÓ` y `CANCELADA`. No se modificaron renderer, CSS, contratos compartidos, Prisma, migraciones, baseline, manifiestos, capturador o comparador; no se atribuyó aprobación al PO y B01–B03 siguen intactos.
- Recurso local conservado y detenido al cierre: `keysar-rv6-p2-pg-20260916-193714`, ligado durante pruebas sólo a `127.0.0.1:55453`, con las bases sintéticas `keysar_rv6_p2`, `keysar_rv6_p2_final` y `keysar_rv6_p2_all`. El secreto fue generado localmente y no se imprimió/versionó; no se leyó `.env`, no se ejecutó seed general ni se tocó una BD compartida. Informe: `docs/pos-automation/runs/2026-09-16T19-37-14-982Z-RV6-P2.md`.

## Checkpoint RV6-P1 (2026-09-16, evidencia histórica anterior — bloqueo documental)

- SHA inicial verificado: `cab113815e63d08cf9e519fe6101a35c34fd8ebc`; referencia: árbol completo `12fb8045cc264b565cb6e764d95ad7b2447fbfa1`.
- `git show` reconfirmó que las props de `MembershipsView` incluyen `onUpdateProfile`, consumo de sesión, agenda y apertura de ticket, pero no una acción de reasignación o cambio manual de estado. `VENDEDOR ACTUAL`, `sellerChanges` y `statusChanges` sólo se renderizan como campos e historial.
- El renderer vigente tampoco invoca los endpoints `/memberships/:id/seller` o `/memberships/:id/status`. Los servicios, rutas, auditoría e historiales backend se conservan; retirarlos o conectarlos sin controles aprobados violaría el alcance.
- Resultado `blocked`: no se recibió decisión nueva de Producto y el contrato prohíbe inventarla. No se modificaron código, contratos, Prisma, baseline, manifiestos, capturador ni comparador; no se ejecutaron pruebas de API/BD, tipos, build o visuales porque el checkpoint es exclusivamente documental.
- No se consultó al PO, no se atribuyó aprobación humana y B01–B03 permanecen sin cambios. No se iniciaron servidores, contenedores o procesos, ni se accedió a `.env`, credenciales o bases de datos. Informe: `docs/pos-automation/runs/2026-09-16T19-33-53-941Z-RV6-P1.md`.

## Checkpoint RV6-P1 (2026-09-16, evidencia histórica anterior — administración representada)

- Inventario contra el árbol completo `12fb804`: el detalle ofrece `Perfilamiento comercial`, asistencia, próxima cita y acceso al ticket; muestra `VENDEDOR ACTUAL` y las listas `sellerChanges`/`statusChanges` sólo como trazabilidad. No contiene selector de nuevo vendedor, motivo, cambio manual de estado ni confirmación para esas mutaciones. La tarjeta de podio sí declara cierre mensual automático.
- En modo API, `MembershipsView` solicita a `App.tsx` el cierre del último mes para `Todas las sucursales` o la sucursal master seleccionada. `App.tsx` traduce nombres a IDs autorizados y reutiliza `createMembershipClosure` con el token personal ya exigido por el módulo. En mock no existe callback y la referencia conserva el cálculo local y el mismo DOM.
- El servicio de cierre mantiene el lock transaccional por mes/alcance y compara el último snapshot completo. Si conteo, total y ranking coinciden devuelve el mismo ID/versión; si cambian crea la siguiente versión append-only. No cambió Prisma, contratos, rutas, cliente compartido, CSS, baseline, capturador ni comparador.
- PostgreSQL 16 dedicado aplicó 46/46 migraciones. `pos-memberships.integration.test.ts` pasó 1/1 y comprobó deduplicación, versión 2 tras añadir una membresía y rechazo `403` no-master. `pos.integration.test.ts` pasó 10/10 e incorporó persistencia/replay idempotente del perfil VIP. También pasaron 136/136 unitarias, lint, tipos, builds API/POS, schemas y manifiesto de 208 capturas.
- Resultado `partial`: reasignar vendedor y cambiar manualmente estado permanecen bloqueados porque requieren nuevos controles/copy. Los endpoints se conservan para no retirar backend ni historiales, pero el renderer no los invoca. Los cambios automáticos por liquidación, asistencia final y cancelación de ticket siguen en sus recorridos ya aprobados. No se consultó ni atribuyó aprobación al PO y B01–B03 no cambiaron.
- Recurso local conservado y detenido: `keysar-rv6-p1-pg-20260916-191550`, ligado durante pruebas sólo a `127.0.0.1:55452`, con datos sintéticos. El secreto se generó localmente y no se imprimió/versionó; no se leyó `.env`, no se ejecutó seed general ni se tocó una BD compartida. Informe: `docs/pos-automation/runs/2026-09-16T19-15-50-093Z-RV6-P1.md`.

## Checkpoint RV5-P1 (2026-09-16, evidencia histórica anterior — membresías y sesiones)

- `planTicketMembershipRevision` bloquea los tarjetones del ticket antes de consumir autorización. Sólo admite el caso derivable del diálogo aprobado: mismos artículos y número de unidades. Agregar, retirar o sustituir tarjetones sigue en `409` porque el formulario no identifica cuál unidad conservar/compensar.
- La migración aditiva `20260916010000_add_pos_membership_revision_projections` crea `PosMembershipRevisionProjection`, historial append-only ligado a `PosTicketEvent` y versión. No se debilitó `PosClientMembership_protect_identity`: importe e identidad originales permanecen inmutables. Vendedor y activación siguen sus historiales canónicos; asistencias y `usedSessions` no se reescriben.
- Una membresía activa con una de dos sesiones consumida rechazó volver a `LAYAWAY` antes del token. El mismo token aplicó después la revisión válida y el replay no duplicó efectos. Una segunda autorización llevó el importe original `500.00` por proyecciones `450.00 → 425.00`, mantuvo una asistencia, `usedSessions = 1`, estado `ACTIVE` y una sola reasignación de vendedor. Los cobros quedaron `SALE/REFUND/REVISION/REFUND/REVISION` por `500/500/450/450/425`.
- Detalle/lista/bootstrap de membresías superponen la última proyección. Exportación autorizada y X-Report leyeron `425.00`; el cierre mensual reutiliza el mismo selector efectivo. La integración PostgreSQL 16 final reconstruyó 46/46 migraciones y pasó 10/10.
- Pasaron tipos de types/API client/API/POS, schemas sincronizados/válidos, lint y 136/136 unitarias, builds API/POS, Prettier, manifiesto visual de 208 capturas y `git diff --check`. No cambió renderer, markup, CSS, baseline, capturador ni comparador; no se repitió la matriz visual completa.
- Resultado `partial`: faltan altas/bajas/sustituciones de membresía sin una unidad identificable, Agenda/capacidad durable, paquetes/cortesías, empresa, adeudos duplicados y certificación transversal restante. B01–B03 y aprobación del PO no cambiaron. Informe: `docs/pos-automation/runs/2026-09-16T18-49-58-408Z-RV5-P1.md`.
- Recursos locales conservados y detenidos: seis PostgreSQL de diagnóstico/verificación ligados durante ejecución sólo a `127.0.0.1:55446–55451`. Contienen datos sintéticos; secretos generados localmente no fueron impresos ni versionados. No se leyó `.env`, no se ejecutó seed general ni se tocó una BD compartida.

## Checkpoint RV5-P1 (2026-09-16, evidencia histórica anterior — adeudos y entregas)

- `appendTicketRevision` reutiliza `PosOwedProduct` y `deliverOwedProduct`; no crea una tabla o historial paralelo. El evento incorpora la diferencia `OWED_PRODUCTS`, cantidades/estados antes y después e IDs de entregas. Una revisión no puede bajar de `deliveredQuantity`; el error ocurre antes de consumir autorización.
- Los adeudos sin inventario comprometido descuentan sólo al entregarse. Al revisarlos, el cálculo físico resta la cantidad aún pendiente, por lo que ampliar un adeudo no crea un `COUNT_ADJUSTMENT` ni duplica el retiro posterior. Los adeudos cuyo inventario quedó comprometido en venta conservan la semántica del ledger existente. Cantidad y estado del adeudo se actualizan en la misma transacción que pagos, legacy, evento y resumen de ticket.
- La integración final sobre PostgreSQL 16 desechable aplicó 45/45 migraciones y pasó 10/10. Rechazó bajar de tres unidades entregadas a dos y usó el mismo token para ampliar a cuatro; el replay mantuvo un solo evento. La recarga mostró pendiente `1.00`, conservó las entregas `1.00/2.00`, saldo de inventario `0.00`, operaciones `SALE/REFUND/REVISION` por `300.00/300.00/400.00` y legacy neto `400.00`. Tras reponer una unidad sintética en la BD de prueba, el motor canónico la entregó y la historia quedó `1.00/2.00/1.00`, total entregado `4.00`, pendiente `0.00` y estado `DELIVERED`.
- Pasaron tipos de types/API client/API/POS, schemas sincronizados/válidos, lint y 136/136 unitarias del API, builds API/POS, Prettier, manifiesto visual de 208 capturas y `git diff --check`. No cambió renderer, markup, CSS, baseline, capturador, comparador, Prisma ni migraciones; no se repitió la matriz visual completa.
- El resultado sigue `partial`. Membresías, Agenda, paquetes/cortesías, empresa y adeudos duplicados ambiguos conservan bloqueo seguro; faltan segundas revisiones y certificación amplia de reportes/exportaciones. No se resolvió B01–B03 ni se atribuyó aprobación al PO. Informe: `docs/pos-automation/runs/2026-09-16T18-35-30-033Z-RV5-P1.md`.
- Recursos locales conservados y detenidos: `keysar-rv5-p1-pg-20260916-owed`, `keysar-rv5-p1-pg-20260916-owed-final` y `keysar-rv5-p1-pg-20260916-owed-verify`, ligados durante ejecución sólo a `127.0.0.1:55440/55441/55445`. Los secretos fueron generados localmente y no se imprimieron/versionaron; no se leyó `.env`, no se usó seed general ni BD compartida.

## Checkpoint RV5-P1 (2026-09-16, evidencia histórica anterior — minorista)

- `PosTicketRevisionRequestDto` y su schema estricto reemplazan el `Record<string, unknown>` de la ruta de revisión. El servidor valida clienta, vendedores, líneas, descuentos, estado/importe de pago y detalles comerciales; PAN/CVV siguen prohibidos. El renderer traduce Visa/Mastercard a IDs autoritativos y, al guardar, vuelve a leer el DTO persistido para refrescar Receipts.
- `appendTicketRevision` bloquea el ticket, reconstruye la cotización con catálogo vigente, consume el token de Receipts dentro de la transacción y crea efectos append-only. Los cobros efectivos previos se compensan mediante `REFUND`; el estado revisado usa `REVISION`; ambas operaciones alimentan `Venta`/`VentaDetalle` y `PosLegacySaleProjection`. El delta de productos usa el ledger existente como `COUNT_ADJUSTMENT`. `PosTicketEvent.snapshot` conserva versión 1 del esquema, versión aplicada, antes/después, diferencias e IDs de las proyecciones; `PosTicket` mantiene el resumen efectivo y la lectura superpone la última revisión sin reescribir líneas históricas.
- La integración sobre PostgreSQL 16 local desechable aplicó 45/45 migraciones y pasó 10/10 casos. Rechazó un ticket con adeudo mediante `409` y luego usó la misma autorización en el ticket apto, acreditando que el bloqueo ocurre antes del consumo. Una venta de dos unidades por `100.00` fue revisada a tres por `120.00`; replay de la misma `Idempotency-Key` devolvió el mismo evento, la recarga mostró total/cobro `120.00`, el ledger dejó existencia `7.00`, las operaciones quedaron `SALE 100.00`, `REFUND 100.00`, `REVISION 120.00`, y la suma de proyecciones legacy fue `120.00` con una sola revisión persistida.
- Pasaron tipos de `types`, cliente, API y POS; schemas Prisma sincronizados/válidos; lint API; 136/136 unitarias; build API/POS; manifiesto visual 25/10/11/10 y 208 capturas; comparación dirigida de etiquetas/acciones de `TicketEditDialog` contra `12fb804`; y `git diff --check`. No se repitió la matriz completa porque no cambió markup ni CSS.
- El resultado es `partial`. Para no publicar incoherencia, el servidor rechaza con `409`, antes de consumir autorización, tickets con adeudos/entregas, membresías, citas/Agenda, paquetes/cortesías o participación comercial de empresa. Faltan los motores compensatorios y pruebas de saldos/reportes para esos casos; el check del plan permanece abierto. No se inventó B01/B02/B03 ni se atribuyó aprobación al PO.
- Recurso local conservado: `keysar-rv5-p1-pg-20260916-180318`, detenido, enlazado sólo a `127.0.0.1:55439` mientras estuvo activo y con datos sintéticos. El secreto se generó localmente, no se imprimió ni versionó; no se usó `.env`, seed general ni BD compartida. Informe: `docs/pos-automation/runs/2026-09-16T18-03-18-954Z-RV5-P1.md`.

## Checkpoint RV5-P2 (2026-09-16, evidencia histórica anterior)

- Se añadió el contrato compatible `PosOwedProductDto.deliveries`; es opcional únicamente para bootstraps offline anteriores. Cada evento nuevo contiene folio, fecha operativa e instante, cantidad, `actorCredentialId`, nombre canónico del actor e `inventoryMovementId`. No cambiaron Prisma, migraciones, la versión offline ni datos existentes.
- `backend/api/src/services/pos-tickets.ts` reutiliza una sola inclusión de ticket para detalle, lista y bootstrap. El historial se arma desde `PosOwedProductDeliveryLine`, ordenado por fecha/ID, sin consultas por fila ni reconstrucción desde cantidades agregadas. El endpoint de entrega devuelve el mismo evento autoritativo.
- `owedProductsFromDto` dejó de vaciar el historial. El diálogo existente de ticket en Receipts agrega una sección condicional con producto, cantidad, fecha y actor usando la clase ya aprobada de historial; Customers e Inventory amplían sus líneas existentes con el actor. Los fixtures del baseline no contienen entregas y no cambian su DOM visible.
- Verificación de esta ejecución: tipos de `types`, API client, API y POS; build de API y POS; lint y 135/135 unitarias del API; schemas Prisma sincronizados/válidos; manifiesto visual 25/10/11/10 y 208 capturas; `git diff --check`, todos en `PASS`.
- La integración dirigida creó una PostgreSQL 16 desechable nueva, aplicó 45/45 migraciones y pasó 10/10 casos POS. El caso RV5-P2 registró 1.00 y 2.00 piezas, repitió la primera solicitud con la misma llave sin duplicarla, confirmó estado final `DELIVERED`, actores/fechas/movimientos y paridad entre detalle, lista y filas persistidas.
- Dos corridas previas sobre bases nuevas detectaron exclusivamente carencias del fixture añadido (ubicación de inventario ausente y lectura con un actor sin `RECEIPTS_VIEW`); se corrigió el fixture para crear su ubicación sintética y consultar con el master autorizado. La tercera base nueva pasó completa. Los tres contenedores `keysar-rv5-p2-pg-20260916-171201`, `keysar-rv5-p2-pg-20260916-attempt2` y `keysar-rv5-p2-pg-20260916-attempt3` quedaron detenidos y conservan sus datos locales; no se usó `.env`, seed general ni una BD compartida.
- No se solicitó ni atribuyó aprobación al PO. B01–B03 y el resto del backlog permanecieron sin cambios en esa ejecución. Informe: `docs/pos-automation/runs/2026-09-16T17-12-01-729Z-RV5-P2.md`.

## Checkpoint RV7-P1 (2026-09-16, evidencia histórica anterior)

- Se modificaron `App.tsx` y `CashManagerView.tsx`. En modo API, el control visible `Código master` valida primero un token efímero `CASH_MANAGER_ACCESS`; el código queda sólo en memoria durante el desbloqueo de tres minutos y se usa para solicitar tokens nuevos `CASH_EXPENSE_EDIT` o `CASH_EXPENSE_VOID`, ligados al `PosCashExpense` exacto. No se agregó alias, diálogo ni otro DOM.
- La corrección ahora espera la respuesta del API antes de cerrar el formulario. Si autorización o persistencia fallan, conserva el formulario y muestra el error del servidor. Anulación mantiene la confirmación aprobada y actualiza estado sólo desde el DTO persistido.
- Se reutilizaron sin cambios los endpoints `POST /api/pos/authorizations`, `POST /api/pos/auth/verify`, `PUT /api/pos/expenses/:id` y `POST /api/pos/expenses/:id/void`, así como su auditoría, idempotencia, compensaciones y tokens de un solo uso. No cambiaron Prisma, migraciones, API client ni contratos compartidos.
- El checkpoint inicial quedó parcial por falta de PostgreSQL; su evidencia histórica permanece en `docs/pos-automation/runs/2026-09-16T08-55-40-652Z-RV7-P1.md`.
- En la reanudación, Podman volvió a responder. Se creó el contenedor dedicado `keysar-rv7-p1-pg-20260916-092804`, enlazado sólo a `127.0.0.1`, con secreto generado localmente, y se aplicaron las 45 migraciones a tres bases nuevas: `keysar_rv7_p1`, `keysar_rv7_p1_full` y `keysar_rv7_p1_all`. No se usó `.env`, seed general ni servicio compartido.
- `backend/api/src/pos.integration.test.ts` añade un recorrido HTTP/BD que comprueba 403 sin `CASH_MANAGE`, desbloqueo fallido/correcto, consumo único de `CASH_MANAGER_ACCESS`, corrección y anulación ligadas al gasto, idempotencia, rechazo de tokens reutilizados, importes exactos, compensaciones con neto cero y recarga persistente.
- Verificación de esta reanudación: 45/45 migraciones; 10/10 casos POS y 16/16 pruebas pertinentes de app/POS/memberships; schemas válidos/sincronizados; tipos/lint/135 unitarias del API; tipos/build web del POS; manifiesto visual, ausencia de `window.prompt`, comparación dirigida del control/copy con `12fb804`, Prettier y `git diff --check`, todos en `PASS`.
- El comando completo `test:integration` se repitió sobre la tercera BD totalmente nueva: app, POS y memberships pasaron 16/16 y el bootstrap Scheduler 1/1; sólo fallaron dos casos Scheduler porque conservan citas fijas del 2026-09-11, ya vencidas. No se borraron datos ni se cambió Scheduler fuera de alcance. La matriz pertinente sobre la segunda BD nueva pasó 16/16.
- B01 sigue diferido y `REGISTRO MOCK · CASH MANAGER` no fue modificado. Informe de cierre: `docs/pos-automation/runs/2026-09-16T09-28-04-169Z-RV7-P1.md`.

## Preparación del ejecutor (2026-09-16)

- Guía de arranque/parada/recuperación: `EJECUTOR_PLAN_POS.md`. Sesión nueva por tarea/checkpoint con plan, handoff, estado e informe versionados antes de commit/push; siguiente sesión sólo después de confirmar el SHA remoto.
- Cola: 34 IDs, 13 técnicos habilitados y 21 diferidos. RV8 conserva la exclusión anterior. Sin PO, completar lo técnicamente posible y documentar por separado requisitos de Windows/hardware/infraestructura; no marcar aceptación humana como aprobada.
- El estado inicial de `docs/pos-automation/state.json` no tenía tareas ejecutadas; actualmente conserva los checkpoints publicados de RV7-P1 y RV5-P2. Las pruebas del ejecutor simulan sesiones y remotos locales; no ejecutan ni acreditan nuevamente los recorridos funcionales del POS.
- Validación vigente del ejecutor: 22/22 pruebas aprobadas con `node --test scripts/run-pos-plan.test.mjs`; `--dry-run` selecciona RV10-P1.
- Incidente/reparación del primer ciclo autónomo: RV7-P1 pasó 10/10 casos POS y 16/16 pruebas pertinentes, pero el ejecutor detuvo la publicación al recibir dos fallos Scheduler ajenos por fechas fijas vencidas. Se recuperó y publicó el cierre después de repetir 10/10 sobre otra base nueva. El contrato distingue ahora `failed_unrelated` con evidencia estricta; los fallos pertinentes siguen deteniendo el proceso. La revisión actualizada pasa 19/19 pruebas del ejecutor, incluida publicación simulada de este caso. Próxima tarea: RV5-P2.
- Incidente/reparación posterior: RV5-P2 terminó, se confirmó tanto local como remotamente en `69880e3`, pero una operación Git superó el timeout local de 120 segundos después de publicar y cortó el bucle antes de RV5-P1. El journal se recuperó sin repetir la implementación. El ejecutor ahora reintenta consultas remotas y, ante timeout de `push`, continúa únicamente si confirma el SHA exacto; si el remoto no lo contiene tras tres intentos o diverge, conserva el estado y se detiene. Próxima tarea: RV5-P1.
- Incidente/reparación de RV9-P3: el checkpoint y sus verificaciones terminaron, pero la allowlist rechazó `scripts/verify-pos-migration-recovery.sh`. Se revisó el helper, se repitieron las verificaciones independientes y se publicó el checkpoint recuperado en `247f360`. La allowlist admite ahora sólo helpers raíz POS con prefijos explícitos; el runner, sus pruebas y scripts generales continúan protegidos. Próxima tarea: RV9-P4.
- Incidente/reparación de RV9-P4: la tarea satisfizo su auditoría y marcó `[x]`, pero mezcló cuatro seguimientos externos en `remaining`, contradiciendo `completed`; se verificó y publicó el cierre recuperado en `c567d0c`. El ejecutor preserva ahora ese caso como `followUps` y continúa sólo cuando el check asignado quedó cerrado y todas las pruebas pertinentes aprobaron. Próxima tarea: RV10-P1.
- Ningún despliegue, provisionamiento, migración o fase funcional se realizó como parte de esta preparación. No confundir los checks históricos siguientes con verificaciones de hoy.

## Evidencia histórica del MVP (2026-09-09)

- PostgreSQL 16, base desechable `keysar_mvp_mvp3`: 45 migraciones y 18/18 pruebas habilitadas.
- API: 25 archivos/135 pruebas unitarias y lint en `PASS`.
- Type-check: types, API client, API y POS en `PASS`.
- Build POS Vite en `PASS`.
- Visual: `/tmp/keysar-pos-mvp-final-candidate-2`; comparación `/tmp/keysar-pos-mvp-final-diff-2/comparison.json`, 208/208 `PASS`, máximo `0.000026235` bajo `0.000027`.

## Qué cambió después de RV4

- Autorizaciones reales sin alterar la UI aprobada en Producto, Receipts, Settings y X-Report.
- Token de Receipts consumible para revisión/cancelación y rechazo de reutilización.
- Identidad completa de clienta creada en Checkout.
- Traducción correcta de Visa/Mastercard entre etiqueta visual e ID del backend.
- Reconstrucción correcta de entrega inicial en apartados desde adeudos.
- Prueba integrada de venta mixta, identidad y cancelación.

## Para continuar desde otra computadora

1. Bajar `feature/pos-frontend-clean` desde `origin`.
2. Leer `CLAUDE.md`, `PLAN_RESTAURACION_VISUAL_POS.md`, este handoff, `docs/pos-automation/state.json` y el último informe enlazado. Para ejecución automática seguir `EJECUTOR_PLAN_POS.md`.
3. No modificar el baseline ni presentar el MVP como listo para producción.
4. Para revisión visual usar fixtures aislados; para revisión funcional preparar el modo API con datos sintéticos persistidos en el entorno autorizado según RV10-P1–P4. El login online inicial actual utiliza Electron. No depender de los directorios `/tmp` de este equipo.
5. Seguir el orden de la sección 6 del plan y cerrar cada check con evidencia. Conservar las decisiones de Producto como diferidas hasta retomar al PO; avanzar el resto, conservar evidencia histórica y probar sólo el alcance afectado.
