# RV10-P3 — bloqueo externo de terminal y entrega privada

- Tarea: `RV10-P3`
- Fecha: `2026-09-17`
- Resultado: `blocked`
- SHA inicial: `1aae14428a90133401726cfe25b1faf45f778042`
- Rama observada: `feature/pos-frontend-clean`
- Referencia visual y de interacción: árbol completo `12fb8045cc264b565cb6e764d95ad7b2447fbfa1`

## Resultado

El checkpoint parcial anterior completó y probó toda la preparación local independiente: registro, activación y reentrada de una terminal sintética, login master/operador, separación de permisos, rechazo de un `Código master` incorrecto y emisión/consumo del correcto mediante las rutas canónicas. Esta reanudación confirmó que el trabajo restante depende exclusivamente de recursos externos que no están en el alcance de la sesión: una terminal real del PO, un canal privado autorizado para entregar credenciales y Windows/WSL para ejecutar la comprobación desde Electron.

Conforme al contrato de ejecución, el resultado es `blocked` y sólo se modificaron documentos. RV10-P3 permanece `[ ]`; no se registró una terminal real, no se entregó ningún secreto y no se atribuye validación o aceptación al PO.

## Referencia y estado inicial

- `git rev-parse HEAD` confirmó el SHA inicial `1aae14428a90133401726cfe25b1faf45f778042` y un árbol limpio al inicio.
- `git show` confirmó la referencia fija `12fb8045cc264b565cb6e764d95ad7b2447fbfa1`, árbol `6f61e86a90927dd19d63c557d65376e0b1eaabed`.
- El commit inicial contiene el helper y la documentación del checkpoint parcial anterior; no se alteraron esos archivos ejecutables.
- `pnpm pos:visual:manifest` confirmó la referencia fija y la matriz de 25 pantallas, 10 secciones de Settings, 11 reportes, 10 vistas de bodega y 208 capturas.

## Bloqueo confirmado

El host es Fedora Linux nativo. No se encontraron `powershell.exe`, `cmd.exe`, `wsl.exe` ni Wine. No se proporcionaron un equipo Windows, una terminal real, un development autorizado o un canal privado autorizado. Usar la terminal sintética `PO-PREP-*` como si fuera real, publicar sus valores o ampliar listeners fuera de loopback violaría el alcance y no cerraría el criterio.

La inspección dirigida confirmó que no queda otra tarea local independiente dentro de RV10-P3:

- `backend/api/scripts/provision-pos-terminal-local.ts` ya exige confirmación literal, API HTTP loopback, dataset coincidente y archivos privados dentro de `.pos-runner/`, y reutiliza las rutas canónicas de terminal, login y autorización.
- `apps/pos/src/main/index.ts` carga `POS_API_URL`, `POS_TERMINAL_CODE` y `POS_TERMINAL_SECRET` en el proceso principal; preload expone `window.electronAPI.posLogin` sin llevar el secreto al renderer.
- REF conserva los controles y mensajes aprobados de `Código master`; no hace falta inventar otra pantalla o campo.

## Recursos locales preservados

- Estado ignorado: `.pos-runner/rv10-p3-local/`, directorio modo `0700`; `config.env`, `credentials.json` y `terminal.json` modo `0600`.
- Contenedor: `keysar-rv10-p1-rv10-p3`, estado `exited`, binding configurado exclusivamente en `127.0.0.1:55472`.
- API: detenida; puertos `4412` y `55472` libres.
- Release conservado por el estado local: `c2bc9c46eacf4e414de677275407cdf12d485a9d`.

No se leyó ni publicó el contenido de los archivos sensibles. No se inició un servidor, no se ejecutó provisionamiento y no se mutó o eliminó la BD sintética.

## Archivos de esta ejecución

- `PLAN_RESTAURACION_VISUAL_POS.md`: evidencia de bloqueo bajo el único check RV10-P3, que continúa `[ ]`.
- `docs/POS_MVP_HANDOFF.md`: estado actual, verificaciones, recursos y siguiente acción.
- Este informe.

`CLAUDE.md` no cambia: no apareció un contrato, módulo, comando o decisión arquitectónica nueva. Tampoco cambiaron API, helper, POS, Prisma, migraciones, dependencias, baseline, manifiestos de referencia, capturador o comparador.

## Verificaciones de esta ejecución

1. `git status --short --branch && git rev-parse HEAD && git show -s --format='%H%n%T%n%s' HEAD && git show -s --format='%H%n%T%n%s' 12fb8045cc264b565cb6e764d95ad7b2447fbfa1`
   - `PASS`: rama asignada limpia al inicio, SHA inicial exacto y referencia/árbol fijo confirmados.
2. `uname -srmo`, `/proc/version` y detección de `powershell.exe`, `cmd.exe`, `wsl.exe`, Wine y Electron del sistema.
   - `PASS`: Fedora Linux nativo; no existe el entorno Windows/WSL requerido para la evidencia restante.
3. `prepare-pos-local-demo.sh status`, `stat`, `podman inspect` y `ss` sobre el estado dedicado RV10-P3.
   - `PASS`: API y PostgreSQL detenidos, permisos `0700`/`0600`, contenedor `exited`, binding loopback y puertos libres.
4. `rg` dirigido sobre el helper, main/preload/renderer y `git show ...:apps/pos/src/renderer/src/App.tsx`.
   - `PASS`: guardas, variables del proceso principal, IPC de login y controles aprobados siguen presentes; no se expusieron valores sensibles.
5. `pnpm pos:visual:manifest`.
   - `PASS`: referencia `12fb804` y matriz 25/10/11/10/208 intactas.
6. Prettier sobre los tres documentos y `git diff --check`.
   - `PASS`: los documentos usan el formato esperado, el diff no contiene errores de whitespace, el informe obligatorio existe y el alcance modificado es exclusivamente documental. RV10-P3 continúa `[ ]`.

No se ejecutaron types, builds, pruebas unitarias, integración HTTP/BD ni comparación por píxel. Este checkpoint bloqueado sólo modifica documentación y no altera código o superficies visibles; las pruebas funcionales pertenecen al informe histórico `docs/pos-automation/runs/2026-09-17T19-12-25-154Z-RV10-P3.md` y no se presentan como nuevas.

## Pendiente dentro de RV10-P3

- Registrar y activar una terminal distinta y real para el PO en el entorno autorizado, conservando su secreto fuera del repositorio y renderer.
- Entregar las credenciales de terminal, master y operador únicamente mediante un canal privado autorizado.
- Comprobar desde Electron/Windows los logins master/operador, sucursal, permisos y autorización con código incorrecto/correcto mediante los controles aprobados.

## Comando para continuar

Cuando existan equipo y canal autorizados, usar un state dir y una terminal distintos con el helper ya documentado. Cargar los valores desde el mecanismo privado del ambiente, nunca desde argumentos o documentación versionada:

```bash
POS_TERMINAL_CONFIRMATION=LOCAL_SYNTHETIC_ONLY \
POS_TERMINAL_DATASET_ID=<dataset-autorizado> \
POS_TERMINAL_API_URL=http://127.0.0.1:<puerto-api> \
POS_TERMINAL_SOURCE_CREDENTIALS_FILE="$PWD/.pos-runner/<estado>/credentials.json" \
POS_TERMINAL_OUTPUT_FILE="$PWD/.pos-runner/<estado>/terminal.json" \
pnpm --dir backend/api exec tsx scripts/provision-pos-terminal-local.ts provision
```

Después, cargar `POS_TERMINAL_CODE` y `POS_TERMINAL_SECRET` en el proceso principal de Electron desde el almacén privado del equipo objetivo y ejecutar `verify` más el recorrido master/operador. No reutilizar la terminal local `PO-PREP-*`, no copiar secretos a la consola y no abrir listeners fuera de la red autorizada.
