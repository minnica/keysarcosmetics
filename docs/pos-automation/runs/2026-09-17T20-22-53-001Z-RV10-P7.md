# RV10-P7 · Bloqueo externo de la revisión Windows/API

- Tarea: `RV10-P7`
- Resultado: `blocked`
- SHA inicial: `6a6dfe3250dbdb338763c3a41aa32b5183877bd3`
- Candidato ejecutable documentado: `06f9f6f7741b733bda7770e3b67b714daa59a40b`
- Rama: `feature/pos-frontend-clean`
- Referencia visual/interacción: `12fb8045cc264b565cb6e764d95ad7b2447fbfa1`
- Árbol de referencia: `6f61e86a90927dd19d63c557d65376e0b1eaabed`
- MVP histórico: `db58fda0aca502f6a543bde03e9fd477b3723caa`
- Host ejecutor: Fedora Linux 44; sin Windows, PowerShell, WSL, Wine ni intervención del PO

## Resultado

La preparación documental útil de RV10-P7 ya quedó publicada en el checkpoint
anterior. `GUIA_PRUEBA_PO_POS_WINDOWS.md` y
`PROMPT_CODEX_PRUEBA_PO_POS_WINDOWS.md` distinguen el fixture visual del
recorrido API, identifican el candidato ejecutable, documentan inicio/parada y
mantienen los secretos fuera del repositorio y de la evidencia. La revisión de
esta sesión confirmó que no prometen que el modo funcional ya esté disponible:
exigen API/BD sintética autorizada, SHA coincidente, CORS, terminal activa,
canal privado y credenciales personales antes de iniciarlo.

No apareció evidencia externa nueva que permita cerrar el criterio. El host
continúa en Linux nativo sin un runtime Windows y no se proporcionaron el equipo
objetivo, una API accesible desde éste, terminal/canal autorizados ni acceso del
PO. Como el mismo bloqueo sucede después de completar la preparación local y
documental independiente, este checkpoint se registra como `blocked` y sólo
modifica documentación. RV10-P7 permanece `[ ]`.

## Archivos modificados

- `PLAN_RESTAURACION_VISUAL_POS.md`: checkpoint de bloqueo únicamente bajo
  RV10-P7; el check continúa abierto.
- `docs/POS_MVP_HANDOFF.md`: estado actual, evidencia auditada, verificaciones
  omitidas y acción exacta para reanudar.
- Este informe durable.

No se modificaron las guías ya publicadas, `CLAUDE.md`, renderer, main/preload,
API, contratos, Prisma, migraciones, dependencias, lockfile, fixtures, baseline,
capturador, comparador, ejecutor o estado de automatización.

## Backend reutilizado/refactorizado

No se cambió backend. La guía funcional sigue remitiendo al flujo existente de
RV10-P1–P4: `GET /health`, `/ready`, CORS, login Electron mediante
`window.electronAPI.posLogin`, terminal/permisos del servidor, autorizaciones
consumibles, auditoría, persistencia e idempotencia. Esta sesión no inició ni
mutó API, PostgreSQL, contenedores o datos.

## Evidencia durable y referencia

- `git show` confirmó la referencia fija `12fb8045cc264b565cb6e764d95ad7b2447fbfa1`
  con árbol `6f61e86a90927dd19d63c557d65376e0b1eaabed`.
- `db58fda0aca502f6a543bde03e9fd477b3723caa` y el candidato ejecutable
  `06f9f6f7741b733bda7770e3b67b714daa59a40b` son ancestros de HEAD.
- Entre `06f9f6f` y HEAD no cambiaron `apps/pos`, `backend/api`,
  `packages/types`, `packages/api-client`, `package.json` o `pnpm-lock.yaml`;
  sólo existen cambios documentales y de estado ya publicados.
- El índice versionado del baseline y sus 208 PNG volvieron a coincidir en
  SHA-256, sin faltantes ni diferencias. Registra Node `22.23.2`, pnpm
  `10.0.0`, Playwright `1.60.0` y Chromium `148.0.7778.96` para la referencia
  `12fb804`.
- El manifiesto confirmó 25 pantallas, 10 secciones de Settings, 11 reportes,
  10 vistas de bodega y 208 capturas.
- La comparación candidata 208/208 del 2026-09-09 sigue siendo un resumen
  histórico de `db58fda`; sus artefactos candidate/diff estaban en `/tmp`. No
  se presenta como una comparación nueva de `06f9f6f` ni como prueba Windows.

## Verificaciones ejecutadas

1. `git show -s --format='commit=%H%nparent=%P%ntree=%T%nsubject=%s' HEAD && git show -s --format='commit=%H%nparent=%P%ntree=%T%nsubject=%s' 12fb8045cc264b565cb6e764d95ad7b2447fbfa1`
   - `PASS`: SHA inicial `6a6dfe3250dbdb338763c3a41aa32b5183877bd3` y
     referencia/árbol fijos confirmados.
2. `git merge-base --is-ancestor db58fda0aca502f6a543bde03e9fd477b3723caa HEAD && git merge-base --is-ancestor 06f9f6f7741b733bda7770e3b67b714daa59a40b HEAD && git diff --quiet 06f9f6f7741b733bda7770e3b67b714daa59a40b HEAD -- apps/pos backend/api packages/types packages/api-client package.json pnpm-lock.yaml`
   - `PASS`: ambas relaciones de ascendencia son válidas y no hay cambios
     ejecutables posteriores al candidato documentado.
3. `uname -srmo` y detección de `powershell.exe`, `pwsh`, `cmd.exe`, `wsl.exe`, `wine`, `wine64` y `/proc/sys/fs/binfmt_misc/WSLInterop`
   - `PASS`: Fedora Linux nativo; todos los runtimes Windows consultados están
     ausentes.
4. `node -e '<verificación SHA-256 de capture-index.json y sus 208 PNG>'`
   - `PASS`: referencia/fuente `12fb804`, 208 entradas, cero archivos ausentes
     y cero hashes discrepantes; runtime del índice confirmado.
5. `pnpm pos:visual:manifest`
   - `PASS`: matriz de referencia 25/10/11/10 y 208 capturas.
6. `node --test scripts/verify-pos-windows-demo.test.mjs && node --check scripts/verify-pos-windows-demo.mjs`
   - `PASS`: 4/4 pruebas de configuración/redacción y sintaxis válida.
7. `ss -H -ltn '( sport = :3005 or sport = :4410 or sport = :4411 or sport = :4412 or sport = :4413 or sport = :55470 or sport = :55471 or sport = :55472 or sport = :55473 or sport = :9223 )'`
   - `PASS`: no existen listeners en los puertos temporales documentados.

## Verificaciones no ejecutadas

- `node scripts/verify-pos-windows-demo.mjs check && node scripts/verify-pos-windows-demo.mjs probe && pnpm --filter @cosmetics/pos dev`: `NOT RUN`; requieren la configuración privada y el equipo Windows/API autorizados que no están disponibles.
- `pnpm pos:visual:capture && pnpm pos:visual:compare`: `NOT RUN`; este checkpoint bloqueado es exclusivamente documental y no atribuye la comparación histórica a una corrida nueva.
- Tipos/build e integración API/BD: `NOT RUN`; no cambió código, contrato,
  schema, migración o dependencia. Los resultados previos conservan su carácter
  histórico.

## Pendiente dentro de RV10-P7

- Ejecutar y registrar la revisión visual en el equipo Windows objetivo,
  incluidos runtime, inicio y parada.
- Completar las dependencias externas de RV10-P1/P3/P4 y ejecutar desde ese
  equipo `docs/POS_WINDOWS_API_DEMO.md`, con SHA/API/terminal autorizados y
  valores privados fuera de la evidencia.
- Registrar por separado el recorrido funcional, recarga/reingreso y
  persistencia. Si se requiere una comparación completa del candidato vigente,
  publicarla como evidencia durable nueva y no reutilizar las rutas `/tmp`.

RV10-P5/P6/P8, B01–B03, RV8 y los gates operativos son seguimientos distintos;
no se presentan como aprobación ni como trabajo ejecutado aquí.

## Recursos y continuación

No se inició servidor, Electron, contenedor o BD. Cuando existan Windows,
API/BD y terminal autorizados, partir del candidato documentado o publicar uno
nuevo, repetir las guardas Git de la guía, seguir
`docs/POS_WINDOWS_API_DEMO.md`, capturar secretos sólo con `Read-Host` y
registrar inicio/reinicio/parada y observaciones sin valores privados. Hasta
entonces la revisión funcional no está disponible para el PO.
