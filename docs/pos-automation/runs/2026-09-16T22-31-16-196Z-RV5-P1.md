# RV5-P1 · Revisiones compensatorias de tickets, checkpoint 8

- Tarea: `RV5-P1`
- Resultado: `partial`
- SHA inicial: `1f9e9dd8de63cb5989eb4ba0a405ad260ecf0b45`
- Rama administrada por el ejecutor: `feature/pos-frontend-clean`
- Referencia visual: árbol completo `12fb8045cc264b565cb6e764d95ad7b2447fbfa1` de `feature/pos`
- Ejecución: 2026-09-16, sin intervención ni aprobación atribuida al PO

## Alcance de este checkpoint

Se cerró la certificación transversal independiente que quedó pendiente en el checkpoint 7:

1. Una segunda revisión se aplica sobre la proyección de la primera, compensa únicamente su cobro efectivo y conserva originales/eventos append-only.
2. Detalle de ventas usa líneas, vendedores y métodos de pago de la última revisión, no concatena líneas originales ni cobros ya compensados.
3. Productos vendidos y rentabilidad usan cantidades, importes, costos, familia y categoría efectivos.
4. Ventas/desempeño por empleado usan el reparto efectivo. Reporte y exportación comparten el mismo dataset.
5. Conciliación bancaria conserva la secuencia completa `SALE/REFUND/REVISION` y calcula el neto con signo.
6. Los periodos sin revisiones conservan el camino paginado anterior; el gate opt-in de 12,000 tickets/24,000 líneas volvió a pasar.

RV5-P1 permanece abierto porque las operaciones restantes no tienen identidad o control aprobado, o exigen recuperación externa durable. No se añadió una interfaz para resolverlas por inferencia.

## Backend reutilizado/refactorizado

- Se reutilizaron `PosTicketEvent.snapshot`, `appendTicketRevision`, `PosPaymentOperation`, `PosLegacySaleProjection`, el ledger de inventario, la idempotencia HTTP y el generador común de datasets de reporte/exportación.
- `appliedTicketRevisionSnapshot` pasó a ser un lector exportado y compartido. Mantiene compatibilidad con eventos anteriores; `itemMetadata` es opcional y los reportes caen a snapshots originales cuando no existe.
- Cada revisión nueva guarda familia/categoría junto con `itemKinds` y `unitCosts`, sin cambiar Prisma ni reescribir `PosTicketLine`/`PosTicketSeller`.
- `salesRows` elige entre dos caminos:
  - sin revisiones en el alcance, conserva consultas SQL paginadas y conteos existentes;
  - con revisiones, superpone el último evento para líneas, vendedores, pagos, filtros y búsqueda antes de paginar el dataset efectivo.
- `BANK_RECONCILIATION` no oculta compensaciones: sigue leyendo el ledger completo y firma `REFUND` como negativo.
- No cambiaron contratos HTTP públicos, Prisma, migraciones, renderer, CSS, assets, fixtures, baseline, manifiestos, capturador, comparador, lockfile ni dependencias.

## Evidencia HTTP/BD

La corrida final usó PostgreSQL 16 dedicado en `keysar-rv5-p1-pg-20260916-reports`, ligado sólo a `127.0.0.1:55467`, y la base nueva `keysar_rv5_p1_reports`. Aplicó 46/46 migraciones desde cero y ejecutó conjuntamente 10/10 casos POS y 3/3 casos POS/Scheduler.

El ticket minorista inició con dos unidades y `SALE 100.00`. La primera revisión lo llevó a tres unidades y `REVISION 120.00`; la segunda, a cuatro y `REVISION 160.00`. Repetir la llave de la segunda devolvió el mismo resultado. Quedaron dos eventos con versiones `2/3`, operaciones `SALE/REFUND/REVISION/REFUND/REVISION` por `100/100/120/120/160`, proyección legacy neta `160.00` e inventario `6.00`.

Tras recarga, el ticket mostró cuatro unidades, total/cobro `160.00`, saldo cero y sólo `REVISION 160.00` como cobro efectivo. `SALES_DETAIL`, `SOLD_PRODUCTS` y `SALES_BY_EMPLOYEE` devolvieron respectivamente cantidad/total/pago, unidades/costo y reparto efectivos. Sus exportaciones fueron idénticas. `BANK_RECONCILIATION` y su exportación conservaron los cinco movimientos y neto `160.00`. Las cuatro exportaciones generaron auditoría.

El gate de carga opt-in volvió a crear datos sintéticos para 1/10/20/30 sucursales, 400 tickets por sucursal, 366 días, 12,000 tickets y 24,000 líneas. Pasó 2/2. A 30 sucursales midió:

- reporte `SALES_DETAIL` completo paginado: `7,226.92 ms`;
- hidratación equivalente de pantalla: `6,626.97 ms`;
- exportación: `2,433.30 ms`;
- máximo de página entre los 13 datasets: `1,097.22 ms` (`BANK_RECONCILIATION`);
- exportaciones de 24,000 filas: `2,024.02 ms` y `2,025.40 ms` para productos/rentabilidad.

Todos quedaron bajo los umbrales técnicos locales `2,000/120,000/30,000 ms`. No constituyen SLA ni aceptación operativa.

## Archivos y contratos

- `backend/api/src/services/pos-tickets.ts`: lector compartido y metadatos efectivos de artículo dentro del evento.
- `backend/api/src/routes/pos-report.routes.ts`: datasets efectivos y camino paginado sin revisiones.
- `backend/api/src/pos.integration.test.ts`: segunda revisión, replay, recarga, ledger, inventario, reportes, exportaciones y auditoría.
- `CLAUDE.md`, `PLAN_RESTAURACION_VISUAL_POS.md`, `docs/POS_MVP_HANDOFF.md` y este informe: contrato operativo y relevo.

## Verificaciones

| Comando                                                                                                                                                                                                   | Resultado | Evidencia                                                                                                                 |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------- |
| `pnpm --filter @cosmetics/api type-check && pnpm --filter @cosmetics/api test:unit`                                                                                                                       | PASS      | TypeScript aprobó; 25 archivos y 136/136 unitarias pasaron.                                                               |
| `DATABASE_URL=<PostgreSQL-16-local-desechable> DIRECT_URL=<misma-BD> pnpm --filter @cosmetics/api exec prisma migrate deploy`                                                                             | PASS      | 46/46 migraciones aplicadas desde cero.                                                                                   |
| `RUN_DATABASE_TESTS=true DATABASE_URL=<misma-BD> DIRECT_URL=<misma-BD> pnpm --filter @cosmetics/api exec vitest run src/pos.integration.test.ts src/pos-scheduler.integration.test.ts --reporter=verbose` | PASS      | 2 archivos y 13/13 casos; segunda revisión, replay, reportes/exportaciones, banco, inventario y proyecciones conciliaron. |
| `RUN_POS_REPORT_LOAD_TESTS=true DATABASE_URL=<misma-BD> DIRECT_URL=<misma-BD> pnpm --filter @cosmetics/api exec vitest run src/pos-report-load.integration.test.ts --reporter=verbose`                    | PASS      | 2/2 casos; 1/10/20/30 sucursales, 12,000 tickets, 24,000 líneas y 13 datasets bajo umbrales.                              |
| `pnpm --filter @cosmetics/api prisma:schemas && DATABASE_URL=<URL-local-sintética> DIRECT_URL=<misma-URL> pnpm --filter @cosmetics/api prisma:validate`                                                   | PASS      | Schemas sincronizados y schema Prisma válido; la URL sintética sólo satisfizo la carga de configuración.                  |
| `pnpm --filter @cosmetics/api lint && pnpm --filter @cosmetics/api build`                                                                                                                                 | PASS      | ESLint sin warnings y build API correcto.                                                                                 |
| `pnpm --filter @cosmetics/types type-check && pnpm --filter @cosmetics/api-client type-check && pnpm --filter @cosmetics/pos type-check`                                                                  | PASS      | Tipos compartidos, cliente y POS compilaron sin errores.                                                                  |
| `pnpm --filter @cosmetics/pos build:web && pnpm pos:visual:manifest`                                                                                                                                      | PASS      | Renderer, main y preload compilaron; referencia `12fb804...` intacta con 208 capturas programadas.                        |
| `pnpm exec prettier --check <archivos-modificados> && git diff --check`                                                                                                                                   | PASS      | Todos los archivos modificados usan el formato esperado y el diff no contiene errores de whitespace.                      |
| `podman stop keysar-rv5-p1-pg-20260916-reports && podman inspect --format '{{.State.Status}}' keysar-rv5-p1-pg-20260916-reports`                                                                          | PASS      | El contenedor devolvió `exited`; conserva sólo la base sintética local.                                                   |
| `pnpm pos:visual:capture && pnpm pos:visual:compare`                                                                                                                                                      | NOT RUN   | No cambiaron renderer, JSX, CSS, assets, fixtures ni baseline; se ejecutaron build y manifiesto proporcionales.           |

## Pendientes y limitaciones

1. Dos adeudos que comparten una misma identidad de línea permanecen en `409` antes del token.
2. Altas, bajas o sustituciones de tarjetones no identifican inequívocamente la unidad desde el diálogo aprobado.
3. Alta, baja, reorden o sustitución de paquete/cortesía y cambios de identidad `COMPANY` carecen de controles aprobados; la composición preservable sí funciona.
4. Reprogramación, cambio de servicio/slot y Agenda HTTP requieren una saga durable y controles aprobados.
5. B01–B03, aprobación del PO, Windows/hardware, RV8 y operación permanecen fuera de este checkpoint y sin decisiones inventadas.

No se leyó `.env`, no se ejecutó el seed general, no se cargaron credenciales operativas y no se tocó ninguna BD compartida. El secreto PostgreSQL se generó localmente, no se imprimió ni versionó. El contenedor conserva únicamente fixtures sintéticos para diagnóstico reproducible y queda detenido.

## Continuación

La siguiente sesión debe volver a auditar los pendientes contra `12fb804`. Si no existe un nuevo control/decisión o un trabajo técnico independiente verificable, registrar RV5-P1 como `blocked` mediante documentación, sin publicar código no verificable. No implementar altas/bajas/sustituciones ni reprogramación por inferencia.

Comandos de relevo:

```bash
git status --short
pnpm --filter @cosmetics/api type-check
pnpm --filter @cosmetics/api test:unit
pnpm --filter @cosmetics/pos type-check
pnpm pos:visual:manifest
```
