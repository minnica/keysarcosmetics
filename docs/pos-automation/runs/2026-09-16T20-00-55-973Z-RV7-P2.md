# RV7-P2 — Certificación técnica de escala de reportes POS

## Resultado

- Tarea: `RV7-P2`.
- Resultado: `completed`.
- SHA inicial: `7c1f69a4310acd55dd25185e813ccbfdf92eefc7`.
- Referencia de presentación/interacción: árbol completo `12fb8045cc264b565cb6e764d95ad7b2447fbfa1` de `feature/pos`.
- Fecha: 2026-09-16, `America/Mexico_City`.

Se certificaron los contratos HTTP/BD de pantalla y archivo con datos sintéticos locales para 1/10/20/30 sucursales. No se accedió a datos productivos, no se ejecutó el seed general, no se usó `.env`, no se desplegó y no se atribuye aceptación al PO. El check acredita un gate reproducible de ingeniería en esta máquina; no define un SLA ni sustituye piloto, hardware o aceptación operativa.

## Alcance y backend reutilizado

Se añadió `backend/api/src/pos-report-load.integration.test.ts`, guardado por `RUN_POS_REPORT_LOAD_TESTS=true`. La prueba reutiliza sin cambios de producción:

- `GET /api/pos/tickets` para la hidratación completa que alimenta la pantalla;
- `GET /api/pos/reports/:key` para el dataset paginado autoritativo;
- `GET /api/pos/exports/:key` para el conjunto completo usado por PDF/XLSX;
- `resolvePosAuthorizedBranches`, `resolvePosReportBranchScope` e históricos asignados;
- permisos `SALE_VIEW_ALL`, `REPORTS_VIEW`, `REPORTS_PRINT` y protección `REPORTS_COSTS`;
- redacción final de columnas sensibles y `AuditLog` de cada exportación.

No fue necesario optimizar las consultas: todas quedaron muy por debajo de los gates fijados. No cambiaron renderer, CSS, `packages/types`, API client, Prisma, migraciones, manifests, lockfile, baseline, fixture visual, capturador ni comparador.

## Datos y umbrales fijados antes de medir

El fixture opt-in genera en una PostgreSQL 16 desechable:

- 30 sucursales, con las posiciones 6, 16 y 26 inactivas pero conservadas en alcance histórico;
- 400 tickets por sucursal y dos líneas por ticket: máximo 12,000 tickets y 24,000 líneas;
- periodo inclusivo de 366 días, `2025-10-01` a `2026-10-01`;
- 120 clientas, 12 artículos, 30 ubicaciones y 360 balances;
- nombres de longitud límite, nombres vacíos y snapshots nulos;
- actor lector sin permiso de costos y actor master para contraste.

Umbrales técnicos predeterminados, configurables por variables opt-in:

| Gate                                       |         Umbral |
| ------------------------------------------ | -------------: |
| Página HTTP de reporte                     |   `< 2,000 ms` |
| Recorrer todas las páginas de un escenario | `< 120,000 ms` |
| Exportación completa                       |  `< 30,000 ms` |

Estos límites son holgados y reproducibles para detectar regresiones graves del entorno local; no son compromisos de capacidad productiva.

## Medición 1/10/20/30

`SALES_DETAIL` recorrió todas las páginas autoritativas de 100 filas y comparó su SHA-256 con el archivo completo. Además, `/tickets` hidrató en páginas de 100 los mismos objetos que usa `ReportsView`; el conjunto de folios de pantalla se comparó exactamente con los folios del archivo.

| Sucursales | Inactivas | Filas/páginas | Reporte completo ms | p95 reporte ms | Hidratación pantalla ms | p95 pantalla ms | Archivo ms | Bytes archivo |
| ---------: | --------: | ------------: | ------------------: | -------------: | ----------------------: | --------------: | ---------: | ------------: |
|          1 |         0 |       400 / 4 |              208.86 |          67.55 |                  186.75 |           50.67 |      92.48 |       267,236 |
|         10 |         1 |    4,000 / 40 |            1,791.63 |          46.87 |                1,749.01 |           45.15 |     769.27 |     2,644,630 |
|         20 |         2 |    8,000 / 80 |            4,105.43 |          55.53 |                3,962.97 |           53.50 |   2,611.71 |     5,380,049 |
|         30 |         3 |  12,000 / 120 |            7,097.17 |          67.25 |                6,640.67 |           60.24 |   2,401.79 |     8,046,118 |

Los cuatro hashes reporte/archivo coincidieron, los folios hidratados por la pantalla fueron exactamente los del archivo, los totales fueron exactos y las sucursales inactivas aportaron 400 filas cada una. El comportamiento no exige una conclusión de crecimiento lineal por tratarse de una sola corrida local; sí confirma amplio margen frente a los gates establecidos.

## Catálogo completo a 30 sucursales

Para cada `POS_REPORT_KEYS` se comparó el total de pantalla, su primera página, el total del archivo y el prefijo equivalente del archivo completo. Todas las llamadas quedaron dentro de umbral.

| Dataset                     |  Filas | Pantalla ms | Archivo ms |
| --------------------------- | -----: | ----------: | ---------: |
| `SALES_DETAIL`              | 12,000 |       51.33 |   2,752.97 |
| `CASH_MOVEMENTS`            | 12,000 |       62.38 |   1,477.11 |
| `SOLD_PRODUCTS`             | 24,000 |       69.22 |   1,896.33 |
| `SALES_BY_EMPLOYEE`         | 12,000 |       60.21 |     770.51 |
| `MERCHANDISE_OVERVIEW`      |    360 |       28.57 |      43.60 |
| `MERCHANDISE_MOVEMENTS`     |      0 |       21.01 |      21.33 |
| `MERCHANDISE_PROFITABILITY` | 24,000 |       62.70 |   2,090.81 |
| `EMPLOYEE_PERFORMANCE`      | 12,000 |       63.26 |     776.78 |
| `EMPLOYEE_DAILY`            | 12,000 |       67.17 |     769.50 |
| `CUSTOMER_OVERVIEW`         |    120 |      405.78 |     529.45 |
| `CUSTOMER_SOURCE_MONTHLY`   |      4 |      200.73 |     206.79 |
| `BANK_RECONCILIATION`       | 12,000 |    1,144.16 |   1,216.18 |
| `INVENTORY_COUNTS`          |    372 |       27.51 |      36.11 |

`MERCHANDISE_MOVEMENTS` queda en cero porque el fixture mide volumen de venta, rentabilidad, cobro, clientas e inventario actual sin inventar movimientos operativos. Su contrato vacío, alcance y exportación sí se ejecutaron y conciliaron.

## Seguridad, históricos y composición de datos

- El lector sin `REPORTS_COSTS` obtuvo `includesCosts=false` y ninguna columna que coincidiera con costo, utilidad, margen o valor de inventario.
- El master obtuvo `includesCosts=true` y las columnas `Costo` y `Utilidad` en rentabilidad.
- El alcance devolvió las 30 IDs solicitadas y marcó correctamente las tres sucursales inactivas.
- Los datasets conservaron `Público general` para snapshot nulo, el nombre largo, la sucursal de nombre vacío y la sucursal de nombre largo. No se añadieron columnas ni se cambió geometría para acomodarlos.
- `AuditLog` registró `branchCount` y `rowCount` del archivo; se comprobó el registro de 30 sucursales y 12,000 filas.
- El renderer y CSS permanecieron sin cambios; type-check/build POS y el manifiesto de la referencia confirmaron 25 pantallas, 10 secciones Settings, 11 reportes, 10 vistas de bodega y 208 capturas. No se repitió la matriz visual porque no cambió ninguna superficie visible.

## Verificaciones

| Comando                                                                                                                                                                                        | Resultado | Evidencia                                                                                                           |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------- |
| `DATABASE_URL=<local-desechable> DIRECT_URL=<misma-BD> pnpm --filter @cosmetics/api exec prisma migrate deploy`                                                                                | PASS      | 46/46 migraciones desde cero en cada base usada para medición/regresión.                                            |
| `RUN_POS_REPORT_LOAD_TESTS=true DATABASE_URL=<local-desechable> DIRECT_URL=<misma-BD> pnpm --filter @cosmetics/api exec vitest run src/pos-report-load.integration.test.ts --reporter=verbose` | PASS      | 2/2 en 60.25 s; tabla completa anterior, incluida hidratación real de pantalla.                                     |
| `RUN_DATABASE_TESTS=true DATABASE_URL=<regresion-local> DIRECT_URL=<misma-BD> pnpm --filter @cosmetics/api test:integration`                                                                   | PASS      | 5 archivos, 21/21; los dos casos de carga POS y el caso de carga Scheduler se omitieron por sus guardas explícitas. |
| `pnpm --filter @cosmetics/api lint && pnpm --filter @cosmetics/api test:unit`                                                                                                                  | PASS      | ESLint sin warnings; 25 archivos y 136/136 pruebas.                                                                 |
| `pnpm --filter @cosmetics/api type-check && pnpm --filter @cosmetics/api build`                                                                                                                | PASS      | TypeScript, Prisma Client y build API completados.                                                                  |
| `pnpm --filter @cosmetics/api prisma:schemas && pnpm --filter @cosmetics/api prisma:validate`                                                                                                  | PASS      | Schemas sincronizados y válidos.                                                                                    |
| `pnpm --filter @cosmetics/pos type-check && pnpm --filter @cosmetics/pos build:web && pnpm pos:visual:manifest`                                                                                | PASS      | Renderer/main/preload construidos; referencia `12fb804`, 208 capturas programadas.                                  |

Tres corridas preparatorias fallaron únicamente dentro del fixture nuevo: nombre de ubicación mayor que 160, código terminal directo sin normalizar a mayúsculas y un extremo de fecha que cubría 365 en vez de 366 días. Cada causa se corrigió en la prueba; no se modificó código de producción y las bases quedaron conservadas, no limpiadas.

## Archivos

- `backend/api/src/pos-report-load.integration.test.ts`
- `PLAN_RESTAURACION_VISUAL_POS.md`
- `docs/POS_MVP_HANDOFF.md`
- `CLAUDE.md`
- `docs/pos-automation/runs/2026-09-16T20-00-55-973Z-RV7-P2.md`

## Recursos, limitaciones y continuidad

El contenedor local `keysar-rv7-p2-pg-20260916-200055` se enlazó sólo a `127.0.0.1:55454`. Conserva `keysar_rv7_p2`, `keysar_rv7_p2_final`, `keysar_rv7_p2_final2`, `keysar_rv7_p2_final3`, `keysar_rv7_p2_final4`, `keysar_rv7_p2_final5` y `keysar_rv7_p2_regression`; las primeras preservan diagnósticos y mediciones intermedias, `final5` la medición definitiva con secretos sintéticos generados al vuelo e hidratación de pantalla, y `regression` la suite integral. El secreto del contenedor se generó localmente y no se imprime ni versiona. El contenedor queda detenido al cierre.

Para repetir en una base PostgreSQL 16 local nueva, aplicar migraciones y ejecutar:

```bash
RUN_POS_REPORT_LOAD_TESTS=true \
DATABASE_URL=<postgresql-local-desechable> \
DIRECT_URL=<misma-base-local> \
pnpm --filter @cosmetics/api exec vitest run \
  src/pos-report-load.integration.test.ts --reporter=verbose
```

Siguiente acción precisa: continuar con RV6-P3 sólo si `AGENDA_PROVIDER=http` se conserva como proveedor o rollback; mantener separados el piloto/hardware y cualquier SLA productivo, que RV7-P2 no acredita.
