# RV10-P4 · Bloqueo externo de la comprobación POS Windows

- Tarea: `RV10-P4`
- Resultado: `blocked`
- SHA inicial: `2bca5e409c3bdf817f77546a6c69dd7d66af31dc`
- Rama: `feature/pos-frontend-clean`
- Referencia visual/interacción: `12fb8045cc264b565cb6e764d95ad7b2447fbfa1`
- Árbol de referencia: `6f61e86a90927dd19d63c557d65376e0b1eaabed`
- Host ejecutor: Fedora Linux 44; sin PowerShell, Windows, WSL, Wine ni intervención del PO

## Resultado de esta ejecución

El criterio continúa bloqueado exclusivamente por dependencias externas. El
equipo Windows objetivo, la terminal/canal privados y el acceso del PO no están
disponibles en esta sesión. Por contrato no se sustituyó esa evidencia con otra
prueba Linux, no se inventó aprobación humana y no se abrió un listener de red,
desplegó un servicio o cargó una credencial operativa.

Esta ejecución sólo actualiza el plan, el handoff y este informe. RV10-P4 sigue
`[ ]`: la preparación local útil pertenece al checkpoint parcial anterior y no
acredita que el PO pueda abrir e ingresar a la demo funcional en Windows.

## Evidencia histórica conservada

El informe `2026-09-17T19-30-40-620Z-RV10-P4.md` registra la preparación ya
publicada: guía PowerShell sin secretos, verificador `check|probe`, prueba local
con PostgreSQL/API sintéticos, compilación web/Electron, dos inicios de Electron
35.7.5 y login/relogin online. También conserva la corrección del primer login
cuando todavía no existía una sucursal seleccionada. Esos resultados no se
presentan como pruebas ejecutadas de nuevo en este checkpoint.

No se cambió backend. La preparación previa reutiliza el API, autenticación,
terminal, permisos, autorizaciones consumibles, auditoría y dataset sintético
existentes; esta sesión no refactorizó contratos, Prisma, migraciones, datos,
outboxes o proyecciones compartidas.

## Comprobaciones de esta ejecución

1. `git branch --show-current && git status --short && git rev-parse HEAD`
   - `PASS`: rama `feature/pos-frontend-clean`, árbol inicialmente limpio y SHA
     exacto `2bca5e409c3bdf817f77546a6c69dd7d66af31dc`.
2. `git show -s --format=... 12fb8045cc264b565cb6e764d95ad7b2447fbfa1`
   - `PASS`: commit fijo presente, árbol
     `6f61e86a90927dd19d63c557d65376e0b1eaabed`; no se modificaron baseline,
     manifiestos, capturador o comparador.
3. `uname -srm`, `/etc/os-release`, búsqueda de PowerShell/Windows/WSL/Wine y
   comprobación de `WSLInterop`
   - `PASS`: Fedora Linux 44 nativo; `powershell.exe`, `pwsh`, `powershell`,
     `cmd.exe`, `wsl.exe`, `wine`, `wine64` y `WSLInterop` están ausentes.
4. Inspección de permisos, contenedor y listeners conservados
   - `PASS`: `.pos-runner/rv10-p4-local/` es `0700`; `config.env`,
     `credentials.json` y `terminal.json` son `0600`; el contenedor
     `keysar-rv10-p1-rv10-p4` está `exited`, ligado sólo a
     `127.0.0.1:55473`; no hay listeners en `3005`, `4413`, `55473` o `9223`.
     No se leyeron ni imprimieron secretos.
5. `node --test scripts/verify-pos-windows-demo.test.mjs && node --check scripts/verify-pos-windows-demo.mjs`
   - `PASS`: 4/4 pruebas validaron configuración coherente y redacción, además
     del rechazo de fixtures, URLs divergentes o inseguras, secretos ausentes,
     placeholders y SHA abreviado; sintaxis válida.
6. `pnpm pos:visual:manifest`
   - `PASS`: referencia `12fb804` con 25 pantallas, 10 secciones de Settings,
     11 reportes, 10 vistas de bodega y 208 capturas programadas.

## Verificaciones no ejecutadas

- `node scripts/verify-pos-windows-demo.mjs check|probe` con configuración real,
  `pnpm --filter @cosmetics/pos dev`, reinicio y login master/operador en
  Windows: `NOT RUN`, porque no existen equipo Windows, API/terminal autorizados
  ni canal privado o acceso del PO en esta sesión. Usar placeholders no sería
  evidencia y el helper los rechaza.
- Tipos/build POS, integración API/BD y Electron Linux: `NOT RUN`; no cambió
  código ejecutable y el checkpoint actual es exclusivamente documental. Los
  resultados del checkpoint parcial anterior se conservan como históricos.
- Comparación visual por píxel: `NOT RUN`; no cambió renderer, CSS, assets,
  fixture, baseline, capturador o comparador. El manifiesto fue suficiente para
  confirmar la referencia afectada.

## Pendiente dentro de RV10-P4

- Ejecutar `docs/POS_WINDOWS_API_DEMO.md` en el equipo Windows objetivo con el
  SHA/API/terminal autorizados y credenciales entregadas por canal privado.
- Registrar inicio y reinicio de Electron, login master y operador, sucursal
  fija, permisos distintos, rechazo/aceptación del campo master aprobado,
  parada y ausencia de listeners sin exponer valores privados.
- Demostrar que el PO puede abrir e ingresar a la demo funcional. La aceptación
  integral del recorrido corresponde a los checks posteriores y no se inventa
  en RV10-P4.

## Recursos y continuación

No se inició ningún servidor o proceso temporal y no se mutó ni eliminó dato
alguno. Se preservan detenidos el contenedor sintético
`keysar-rv10-p1-rv10-p4`, su BD local y `.pos-runner/rv10-p4-local/`; no
reutilizarlos como credenciales del equipo objetivo ni con otro SHA/dataset.

Cuando exista el entorno autorizado, seguir íntegramente
`docs/POS_WINDOWS_API_DEMO.md`: cargar los secretos mediante `Read-Host` en la
misma sesión PowerShell, ejecutar `check` y `probe`, tipos/build, iniciar y
reiniciar Electron, completar las observaciones indicadas y limpiar variables.
No ampliar bindings, desactivar CORS, borrar caché/outbox o copiar secretos para
hacer pasar la prueba.
