# RV6-P1 — administración de membresías representada en `12fb804`

## Resultado

- Tarea: `RV6-P1`.
- Resultado: `partial`.
- Rama administrada por el ejecutor: `feature/pos-frontend-clean`.
- SHA inicial verificado: `1c4b1f6f90a14a9df7154ed85de8946e0440bc60`.
- Referencia visual e interacción: árbol completo `12fb8045cc264b565cb6e764d95ad7b2447fbfa1` de `feature/pos`.
- No se consultó al PO, no se inventó B01/B02/B03 y no se atribuyó aprobación humana.

## Inventario contra la referencia

`git grep` y `git show` sobre `12fb804` confirmaron que `MembershipsView` representa estas operaciones o salidas administrativas:

1. `Perfilamiento comercial`: `Select` editable dentro del expediente; ya tenía destino canónico `POST /api/pos/memberships/:id/profile`.
2. `CIERRE MENSUAL · TOP 3 VENDEDORES`: tarjeta de salida cuyo copy indica que el podio se actualiza automáticamente al cerrar el mes; el backend ya tenía `POST /memberships/closures` y snapshots/rankings append-only.
3. `VENDEDOR ACTUAL`, `sellerChanges` y `statusChanges`: son campos e historial de sólo lectura. El árbol no contiene selector de nuevo vendedor, captura de motivo, cambio manual de estado ni confirmación para esas mutaciones.

Confirmar asistencia, agendar próxima sesión, activación por liquidación, agotamiento tras la última asistencia y cancelación mediante el ticket pertenecen a recorridos ya representados y conservados. No se añadieron nuevas pantallas, diálogos, campos, botones ni copy.

## Cambios implementados

- `apps/pos/src/renderer/src/components/MembershipsView.tsx`
  - Solicita el cierre del último mes sólo para un master autorizado y para el alcance seleccionado (`Todas` o una sucursal).
  - Proyecta `rankings` del snapshot canónico sobre la tarjeta existente, sin cambiar su DOM. Si no existe callback —modo mock/baseline— conserva el cálculo histórico de la referencia.
- `apps/pos/src/renderer/src/App.tsx`
  - Traduce nombres visibles de sucursal a IDs del catálogo autorizado.
  - Reutiliza el token personal vigente del módulo y `createMembershipClosure`; no persiste ni expone claves.
- `backend/api/src/services/pos-memberships.ts`
  - Conserva el advisory lock por mes/alcance.
  - Compara el último snapshot con conteo, total y ranking actuales. Devuelve el mismo ID/versión si no hubo cambio y crea una versión nueva sólo ante contenido distinto.
  - Sigue excluyendo membresías canceladas y usa `effectiveMembershipPurchaseAmount`, por lo que respeta proyecciones de revisión sin reescribir originales.
- `backend/api/src/pos-memberships.integration.test.ts`
  - Comprueba deduplicación exacta, creación de versión 2 tras una membresía adicional y rechazo de cierre para actor no-master.
- `backend/api/src/pos.integration.test.ts`
  - Comprueba que el perfil visible persiste como `VIP` y que repetir la misma `Idempotency-Key` devuelve el mismo resultado.

No cambiaron Prisma, migraciones, contratos públicos, API client, estilos, fixtures, baseline, capturador ni comparador.

## Backend reutilizado/refactorizado

- Reutilizado: autorización personal `MEMBERSHIPS_ACCESS`, permiso `MEMBERSHIPS_MANAGE`, scope de sucursales, `PosMembershipSalesClosure`, rankings, idempotencia HTTP y auditoría/rutas existentes.
- Refactor mínimo: deduplicación semántica dentro de `createMembershipClosure`; no se creó una segunda fuente de verdad.
- Conservado sin conexión visual: `changeMembershipSeller` y `changeMembershipStatus`. Sus endpoints e historiales no se eliminaron porque existen datos/consumidores canónicos, pero llamarlos desde el renderer exigiría controles que `12fb804` no define.

## Verificaciones

- `git rev-parse HEAD` — `PASS`; devolvió el SHA inicial asignado.
- `DATABASE_URL=<PostgreSQL-16-local-desechable> DIRECT_URL=<misma-BD> pnpm --filter @cosmetics/api exec prisma migrate deploy` — `PASS`; 46/46 migraciones desde cero.
- `RUN_DATABASE_TESTS=true DATABASE_URL=<local-desechable> DIRECT_URL=<misma-BD> pnpm --filter @cosmetics/api exec vitest run src/pos-memberships.integration.test.ts` — `PASS`; 1/1, incluida repetición sin versión duplicada, versión 2 ante cambio y `403` no-master.
- `RUN_DATABASE_TESTS=true DATABASE_URL=<local-desechable> DIRECT_URL=<misma-BD> pnpm --filter @cosmetics/api exec vitest run src/pos.integration.test.ts` — `PASS`; 10/10, incluido perfilamiento persistido e idempotente.
- `pnpm --filter @cosmetics/types type-check` — `PASS`.
- `pnpm --filter @cosmetics/api-client type-check` — `PASS`.
- `pnpm --filter @cosmetics/api type-check` — `PASS`.
- `pnpm --filter @cosmetics/pos type-check` — `PASS`.
- `pnpm --filter @cosmetics/api lint` — `PASS`, sin warnings.
- `pnpm --filter @cosmetics/api test:unit` — `PASS`, 25 archivos y 136/136 pruebas.
- `pnpm --filter @cosmetics/api prisma:schemas` — `PASS`, schemas sincronizados.
- `DATABASE_URL=<local-no-operativa> DIRECT_URL=<misma-URL> pnpm --filter @cosmetics/api prisma:validate` — `PASS`, sin conexión ni mutación de BD.
- `pnpm --filter @cosmetics/api build` — `PASS`.
- `pnpm --filter @cosmetics/pos build:web` — `PASS`; renderer, main y preload.
- `pnpm pos:visual:manifest` — `PASS`; referencia fija, 25 pantallas, 10 secciones Settings, 11 reportes, 10 vistas de bodega y 208 capturas programadas.
- Comparación dirigida de fuente mediante `git show`/`git grep` — `PASS`; no se añadió un control y el modo mock no recibe el callback nuevo. No se repitió la matriz completa porque no cambiaron JSX visible, CSS, fixtures ni baseline; sólo la procedencia de los datos de la tarjeta en modo API.
- `git diff --check` — `PASS`.

## Pendientes y limitaciones

- Reasignar vendedor requiere al menos selección de destino, motivo y confirmación. Ninguno existe en `12fb804`; queda bloqueado como iniciativa de Producto.
- Cambiar manualmente estado requiere definir estados permitidos, motivo y confirmación. La referencia sólo presenta el historial; queda bloqueado.
- No se añadió un botón de recierre. La tarjeta aprobada dispara el cierre automático y el servidor versiona únicamente cuando su contenido cambia.
- El check del plan permanece `[ ]`: este checkpoint publica el trabajo independiente probado, pero no convierte las dos exclusiones visuales en funciones implementadas ni en decisiones aprobadas.
- RV6-P2 (incidencias Scheduler interno) es independiente y puede continuar sin esperar estas decisiones.

## Recursos locales y continuación

- PostgreSQL dedicado: `keysar-rv6-p1-pg-20260916-191550`, detenido, con datos sintéticos conservados; durante las pruebas estuvo ligado sólo a `127.0.0.1:55452`.
- El secreto fue generado localmente, no se imprimió ni versionó. No se leyó `.env`, no se ejecutó el seed general y no se tocó una BD compartida.
- Para repetir la integración, iniciar ese contenedor o crear otra PostgreSQL 16 local desechable, aplicar las 46 migraciones y ejecutar los dos comandos Vitest anteriores con `RUN_DATABASE_TESTS=true`.
- Próxima acción de RV6-P1: cuando Producto defina controles compatibles con la referencia, conectar `/seller` y `/status` con motivo, autorización, historial y pruebas HTTP/BD. Hasta entonces no invocar esas rutas desde el renderer. Próxima tarea técnica independiente: `RV6-P2`.
