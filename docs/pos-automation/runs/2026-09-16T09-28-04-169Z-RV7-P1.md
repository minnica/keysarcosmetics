# Informe RV7-P1 — cierre de autorización de gastos

- Tarea: `RV7-P1`
- Resultado: `completed`
- Fecha: 2026-09-16
- SHA inicial: `743c8748ac81c0bf47f741681bace7adddedb5a0`
- Referencia visual: árbol completo `12fb8045cc264b565cb6e764d95ad7b2447fbfa1` de `feature/pos`
- Rama administrada por el ejecutor: `feature/pos-frontend-clean`

## Objetivo y resultado

Se cerró la validación que faltaba al checkpoint anterior. Cash Manager conserva el control aprobado `Código master`, sin alias, PIN adicional, diálogo nuevo ni `window.prompt`. En modo API el desbloqueo valida y consume una autorización `CASH_MANAGER_ACCESS`; cada corrección o anulación emite después un token distinto, de un solo uso y ligado a sesión, terminal, propósito y gasto.

La persistencia se comprobó contra PostgreSQL 16 local y desechable. El recorrido HTTP/BD valida permiso `CASH_MANAGE`, código incorrecto/correcto, idempotencia, importes, movimientos compensatorios, recarga y rechazo de reutilización. RV7-P1 queda completado. RV3-B01 permanece diferido y su copy no cambió.

## Archivos cambiados

- `backend/api/src/pos.integration.test.ts`
  - Añade el caso dirigido `persiste correcciones y anulaciones de gastos con autorización consumible`.
  - Comprueba 403 para un operador sin `CASH_MANAGE`.
  - Consume una autorización de desbloqueo `CASH_MANAGER_ACCESS` y rechaza su segundo uso.
  - Crea un gasto de `125.40`, repite la misma clave idempotente sin duplicarlo, lo corrige a `140.55` y repite la corrección sin duplicar reemplazo ni movimientos.
  - Anula el reemplazo, comprueba una repetición idempotente y rechaza los tokens consumidos al intentar aplicarlos a otro gasto activo.
  - Verifica directamente en Prisma el original anulado, el reemplazo enlazado, cuatro movimientos `EXPENSE`/`VOID`/`CORRECTION`/`VOID` y neto `0.00`; después recupera los estados por `GET /api/pos/expenses`.
- `PLAN_RESTAURACION_VISUAL_POS.md`
  - Marca únicamente RV7-P1 como `[x]` y conserva por separado el checkpoint parcial histórico.
- `docs/POS_MVP_HANDOFF.md`
  - Registra el cierre, la evidencia de esta reanudación, la limitación de la suite Scheduler y la siguiente tarea del plan.
- `CLAUDE.md`
  - Actualiza el estado contractual de RV7-P1 y enlaza este informe.
- `docs/pos-automation/runs/2026-09-16T09-28-04-169Z-RV7-P1.md`
  - Este informe de cierre.

`apps/pos/src/renderer/src/App.tsx` y `apps/pos/src/renderer/src/components/CashManagerView.tsx` no necesitaron cambios adicionales en esta reanudación: el SHA inicial ya contenía la sustitución de prompts y el flujo asíncrono documentados en el checkpoint anterior.

## Backend reutilizado/refactor

No se modificaron rutas, servicios, Prisma, migraciones, API client ni contratos compartidos. La prueba recorre el backend existente:

- `POST /api/pos/authorizations` para resolver el código visible sin alias y emitir autorizaciones ligadas.
- `POST /api/pos/auth/verify` para consumir el token de desbloqueo.
- `POST /api/pos/expenses` con idempotencia y permiso `CASH_MANAGE`.
- `PUT /api/pos/expenses/:id` para anular el original, registrar la compensación y crear el reemplazo trazable.
- `POST /api/pos/expenses/:id/void` para anular mediante compensación auditada.
- `GET /api/pos/expenses` para recuperar los DTO persistidos.
- `executePosIdempotent` y `consumeOperationAuthorization` para convergencia y consumo único ligado a sesión, terminal, propósito y entidad.

Se preservaron auditoría, outboxes, proyecciones y datos. No se ejecutó el seed general ni se conectó a una BD compartida.

## PostgreSQL local desechable

Podman volvió a estar operativo en esta sesión y ya disponía de la imagen local `docker.io/library/postgres:16-alpine`. Se creó el contenedor `keysar-rv7-p1-pg-20260916-092804` con estas guardas:

- puerto publicado únicamente en `127.0.0.1`;
- usuario y bases dedicados a RV7-P1;
- contraseña aleatoria generada localmente, no impresa ni versionada;
- ninguna lectura de `.env` y ninguna conexión a servicios externos.

Se conservaron tres bases nuevas, sin limpiar datos:

- `keysar_rv7_p1`: ejecución dirigida inicial y el intento posterior de la suite completa;
- `keysar_rv7_p1_full`: reconstrucción limpia usada para la matriz pertinente final.
- `keysar_rv7_p1_all`: reconstrucción limpia usada para aislar el resultado de `test:integration` completo.

Las tres recibieron las 45 migraciones vigentes mediante `prisma migrate deploy`. El contenedor se detuvo al finalizar y se conserva para trazabilidad; no se eliminó.

## Verificaciones ejecutadas

1. `DATABASE_URL=<local-desechable> DIRECT_URL=<local-desechable> pnpm exec prisma migrate deploy`
   - `PASS`: 45/45 migraciones aplicadas desde cero en `keysar_rv7_p1`.
2. `RUN_DATABASE_TESTS=true DATABASE_URL=<local-desechable> DIRECT_URL=<local-desechable> pnpm exec vitest run src/pos.integration.test.ts`
   - `PASS`: 10/10 pruebas POS. El caso RV7-P1 pasó y dejó evidencia de permiso, persistencia, importes, compensaciones, idempotencia y tokens consumibles.
3. `RUN_DATABASE_TESTS=true DATABASE_URL=<primera-bd-ya-usada> DIRECT_URL=<primera-bd-ya-usada> pnpm --filter @cosmetics/api test:integration`
   - `FAIL`: la segunda ejecución POS sobre la misma BD chocó con la huella global del PIN ya provisionado; dos pruebas Scheduler rechazaron citas fijas del 2026-09-11, fecha ya pasada el día de esta ejecución. `app.integration` y memberships sí pasaron. No se borraron fixtures ni se cambió Scheduler fuera de alcance.
4. `DATABASE_URL=<segunda-local-desechable> DIRECT_URL=<segunda-local-desechable> pnpm exec prisma migrate deploy`
   - `PASS`: 45/45 migraciones aplicadas desde cero en `keysar_rv7_p1_full`.
5. `RUN_DATABASE_TESTS=true DATABASE_URL=<segunda-local-desechable> DIRECT_URL=<segunda-local-desechable> pnpm exec vitest run src/app.integration.test.ts src/pos.integration.test.ts src/pos-memberships.integration.test.ts`
   - `PASS`: 3 archivos y 16/16 pruebas pertinentes sobre la BD nueva.
6. `RUN_DATABASE_TESTS=true DATABASE_URL=<tercera-local-desechable> DIRECT_URL=<tercera-local-desechable> pnpm --filter @cosmetics/api test:integration`
   - `FAIL`: sobre una BD totalmente nueva pasaron app, POS y memberships (16/16) y el bootstrap Scheduler (1/1); sólo fallaron dos casos de `scheduler-appointments.integration.test.ts` porque intentan crear citas fijas del 2026-09-11, fecha ya pasada. El load test permaneció omitido por su guard propia.
7. `pnpm --filter @cosmetics/api test:unit`
   - `PASS`: 25 archivos y 135/135 pruebas.
8. `pnpm --filter @cosmetics/api lint`
   - `PASS`: ESLint sin advertencias ni errores.
9. `pnpm --filter @cosmetics/api type-check`
   - `PASS`: TypeScript sin errores.
10. `pnpm --filter @cosmetics/api prisma:schemas`

- `PASS`: ambos schemas Prisma están sincronizados.

11. `DATABASE_URL=<local-desechable> DIRECT_URL=<local-desechable> pnpm --filter @cosmetics/api prisma:validate`
    - `PASS`: schema Prisma válido. El primer intento sin `DIRECT_URL` falló por configuración ausente y se repitió con la URL local explícita.
12. `pnpm --filter @cosmetics/pos type-check`
    - `PASS`: TypeScript sin errores.
13. `pnpm --filter @cosmetics/pos build:web`
    - `PASS`: renderer, main y preload compilaron; sólo apareció el aviso histórico de chunk mayor a 500 kB.
14. `pnpm pos:visual:manifest`
    - `PASS`: referencia `12fb804`, 25 pantallas y 208 capturas programadas; no se modificó el baseline.
15. `if rg -n "window\\.prompt" apps/pos/src/renderer/src; then exit 1; else echo PASS; fi`
    - `PASS`: no existe `window.prompt` en el renderer POS.
16. Comparación dirigida con `git show 12fb804...:apps/pos/src/renderer/src/components/CashManagerView.tsx` y `rg -F` sobre el candidato.
    - `PASS`: `Historial protegido`, su explicación, `Código master`, su `aria-label`, `Desbloquear` y `REGISTRO MOCK · CASH MANAGER` coinciden. Fue una comparación de contrato visible/copy, no una nueva comparación por píxel.
17. `pnpm exec prettier --check backend/api/src/pos.integration.test.ts apps/pos/src/renderer/src/App.tsx apps/pos/src/renderer/src/components/CashManagerView.tsx && git diff --check`
    - `PASS`: formato y whitespace correctos antes del cierre documental.

## Pendientes y limitaciones

- No queda trabajo requerido para RV7-P1.
- RV3-B01 sigue abierto: no se cambió ni se atribuyó aprobación al copy `REGISTRO MOCK · CASH MANAGER`.
- No se repitió la matriz visual completa ni se regeneró evidencia. El cambio nuevo es una prueba backend; la superficie visible heredada se comparó de forma dirigida con la referencia fija.
- El comando completo `test:integration` sobre una BD nueva no queda verde en la fecha actual exclusivamente porque Scheduler usa citas fijas del 2026-09-11. En esa misma ejecución app, POS y memberships pasaron 16/16 y el bootstrap Scheduler 1/1; la matriz pertinente separada pasó 16/16.
- El contenedor local detenido y sus tres bases se conservan. No contienen datos reales, credenciales operativas ni secretos versionados.

## Comandos para continuar

Para inspeccionar el recurso local sin imprimir su contraseña:

```bash
podman inspect keysar-rv7-p1-pg-20260916-092804 --format '{{.Name}} {{.ImageName}} {{.State.Status}}'
podman start keysar-rv7-p1-pg-20260916-092804
podman exec keysar-rv7-p1-pg-20260916-092804 pg_isready -U keysar_rv7 -d keysar_rv7_p1_full
```

Para volver a ejecutar pruebas mutantes, crear otra base dedicada en este contenedor y aplicar migraciones desde cero; no reutilizar las tres bases conservadas ni obtener la URL desde `.env`. Recuperar la contraseña sólo dentro del proceso local desde la configuración del contenedor y no imprimirla.

Después de este cierre, continuar con RV5-P2/P1 y RV6-P1/P2 según la sección 6 de `PLAN_RESTAURACION_VISUAL_POS.md`.
