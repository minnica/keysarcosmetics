# RV10-P1 — Bloqueo de comprobación externa Windows/development

- Tarea: `RV10-P1`
- Fecha: 2026-09-17
- Resultado: `blocked`
- SHA inicial: `f7fc12037fe0c8815b78315cf8ac9d9f86c57c44`
- Rama observada: `feature/pos-frontend-clean`
- Referencia visual y de interacción: árbol completo `12fb8045cc264b565cb6e764d95ad7b2447fbfa1`

## Resultado

La preparación local reproducible de API y PostgreSQL ya estaba implementada y probada en el checkpoint parcial anterior. Esta reanudación confirmó que el único requisito pendiente de RV10-P1 depende de un recurso externo ausente: comprobar desde Windows/WSL autorizado, o desde un environment development aislado autorizado, la URL/configuración del candidato, `health.release`, `/ready` y CORS.

El host disponible es Fedora Linux nativo. No contiene `powershell.exe`, `cmd.exe` ni `wsl.exe`, y no se proporcionó un development autorizado. No se amplió el binding loopback, no se desplegó, no se expusieron puertos a Internet y no se mutó ninguna base. Conforme al contrato de la sesión, el resultado bloqueado sólo modifica documentación y RV10-P1 permanece `[ ]`.

## Referencia y manifiestos inspeccionados

- `git show -s --format='%H %T %s' 12fb8045cc264b565cb6e764d95ad7b2447fbfa1` confirmó el commit `feat(pos): add new modules` y el árbol `6f61e86a90927dd19d63c557d65376e0b1eaabed`.
- `apps/e2e/pos-visual/reference-fixture.json` fija ese SHA, los hashes de fuente/activos, el runtime y la política sin API/BD para evidencia visual.
- `apps/e2e/pos-visual/reference-scenarios.json` conserva el mismo SHA y la matriz canónica. No se cambiaron referencia, baseline, fixtures, capturador ni comparador.

## Archivos de esta ejecución

- `PLAN_RESTAURACION_VISUAL_POS.md`: añade el bloqueo debajo del único check RV10-P1; el check permanece abierto.
- `docs/POS_MVP_HANDOFF.md`: separa esta reanudación bloqueada de la evidencia histórica del checkpoint parcial.
- Este informe obligatorio.

`CLAUDE.md` no cambia: no apareció un contrato, módulo, comando o decisión arquitectónica nueva. `docs/pos-automation/state.json` no cambia porque pertenece al ejecutor.

## Backend reutilizado y estado local

No se refactorizó backend en esta ejecución. Se conserva el helper existente `backend/api/scripts/prepare-pos-local-demo.sh`, el listener `HOST`, Express, `/health`, `/ready`, CORS, Prisma y las 46 migraciones versionadas verificadas en la ejecución anterior.

El recurso local anterior se inspeccionó sin iniciarlo ni mutarlo:

- contenedor `keysar-rv10-p1-pg`: `exited`;
- binding configurado: `127.0.0.1:55470`;
- API: detenida; puerto `4410` libre;
- PostgreSQL: detenido; puerto `55470` libre;
- estado ignorado: `.pos-runner/rv10-p1-local/`;
- `config.env` y `empty.env`: modo `0600`;
- release conservado: `27b92cacc195f48020033940166a9f5f6460522b`.

Ese estado está ligado al SHA anterior y debe conservarse. El helper rechaza correctamente reutilizarlo para otro SHA; una futura preparación de `f7fc12037fe0c8815b78315cf8ac9d9f86c57c44` debe usar otro `POS_DEMO_STATE_DIR`, nombre de contenedor y puertos locales.

## Verificaciones de esta ejecución

1. `git status --short --branch && git rev-parse HEAD`
   - `PASS`: árbol inicial limpio en `f7fc12037fe0c8815b78315cf8ac9d9f86c57c44` y rama asignada.
2. `git show -s --format='%H %T %s' 12fb8045cc264b565cb6e764d95ad7b2447fbfa1`
   - `PASS`: referencia fija y árbol confirmados.
3. `uname -a && sed -n '1,3p' /proc/version && command -v powershell.exe || true && command -v cmd.exe || true && command -v wsl.exe || true`
   - `PASS`: Fedora Linux nativo; no existe un runtime Windows/WSL disponible.
4. `backend/api/scripts/prepare-pos-local-demo.sh status && bash -n backend/api/scripts/prepare-pos-local-demo.sh`
   - `PASS`: API/BD anteriores detenidas, release conservado y sintaxis Bash válida.
5. `stat -c '%a %n' .pos-runner/rv10-p1-local/config.env .pos-runner/rv10-p1-local/empty.env`
   - `PASS`: ambos archivos conservan permiso `0600`; no se leyeron ni imprimieron secretos.
6. `podman inspect --format '<nombre estado binding>' keysar-rv10-p1-pg`
   - `PASS`: contenedor detenido y binding configurado exclusivamente en `127.0.0.1:55470`.
7. Comprobación con `ss` de los puertos `4410` y `55470`
   - `PASS`: no quedó ningún listener activo.

No se ejecutaron tipos, builds, unitarias, integración HTTP/BD ni comparación visual: esta ejecución bloqueada no cambió código, contratos, schema, migraciones, fixtures o superficies visibles. Los resultados del checkpoint `2026-09-17T18-28-31-657Z-RV10-P1` son evidencia histórica y no se atribuyen a esta corrida.

## Pendiente dentro de RV10-P1

- Preparar el candidato desde Windows/WSL autorizado o en un development aislado autorizado y comprobar desde el entorno consumidor la URL/configuración, `health.release`, `/ready` y CORS.
- Si loopback no es alcanzable desde Windows, definir explícitamente una interfaz o red privada confiable y sus controles antes de ampliar el binding. Este checkpoint no autoriza `0.0.0.0`, cambios de firewall, túneles, despliegues ni exposición a Internet.

RV10-P2 y tareas posteriores no forman parte del pendiente de este criterio y no se iniciaron.

## Comandos para continuar

En un entorno autorizado, preparar el SHA actual sin reutilizar el estado anterior:

```bash
POS_DEMO_STATE_DIR="$PWD/.pos-runner/rv10-p1-f7fc120" \
POS_DEMO_POSTGRES_CONTAINER=keysar-rv10-p1-pg-f7fc120 \
POS_DEMO_DB_PORT=55471 \
POS_DEMO_API_PORT=4411 \
POS_DEMO_CONFIRMATION=LOCAL_SYNTHETIC_ONLY \
POS_DEMO_EXPECTED_SHA=f7fc12037fe0c8815b78315cf8ac9d9f86c57c44 \
backend/api/scripts/prepare-pos-local-demo.sh up
```

Con las mismas variables no secretas, ejecutar `verify`, registrar desde Windows o development la URL sanitizada, SHA devuelto, readiness y preflight CORS, y finalizar con `down`. No documentar credenciales ni marcar `[x]` sin esa evidencia externa.
