# RV6-P2 — incidencias del proveedor Scheduler interno

## Resultado

- Tarea: `RV6-P2`.
- Resultado: `completed`.
- SHA inicial: `b54288c902f914c3a2c95f130fc7ecb3845ab34f`.
- Rama administrada por el ejecutor: `feature/pos-frontend-clean`.
- Referencia visual/interactiva: árbol completo `12fb8045cc264b565cb6e764d95ad7b2447fbfa1` de `feature/pos`.

Scheduler interno quedó comprobado como autoridad de disponibilidad, capacidad y citas del POS. Las proyecciones POS/Agenda, los beneficios reservados y el saldo de membresía concilian en creación, reintento, asistencia, no-show y cancelación. No se cambió la presentación aprobada ni se inventó una decisión de Producto.

## Cambios

- `backend/api/src/scheduler-appointments.integration.test.ts`: reemplaza instantes fijos ya vencidos por viernes futuros derivados y conserva las pruebas de autenticación, replay/conflicto y último lugar concurrente.
- `backend/api/src/pos-scheduler.integration.test.ts`: añade un fixture sintético aislado y dos recorridos HTTP/BD. Cubre disponibilidad interna, clienta nueva/existente, cortesía simple/doble, atomicidad sin reserva parcial, próxima sesión, replay, `ATTENDED`, `NO_SHOW`, cancelación y consumo único.
- `backend/api/src/services/pos-agenda.ts`: valida que el recurso interno pertenece al mismo servicio solicitado. Para próxima sesión resuelve desde el slot el perfil/artículo `SERVICE` real, y lo conserva en la reserva, confirmación y `PosAppointment` en vez de confundirlo con el artículo `MEMBERSHIP`.
- `backend/api/src/routes/scheduler-appointments.routes.ts`: `NO_SHOW`, igual que cancelación, libera beneficios que seguían `RESERVED`; sólo `ATTENDED` pasa a `CONSUMED` y crea la asistencia.
- `PLAN_RESTAURACION_VISUAL_POS.md`, `docs/POS_MVP_HANDOFF.md` y `CLAUDE.md`: registran el cierre y el contrato corregido.

No cambiaron Prisma, migraciones, contratos compartidos, renderer, markup, CSS, baseline, manifiestos de referencia, capturador, comparador, dependencias ni lockfile.

## Backend reutilizado y conciliación

- Se reutilizaron `refreshAgendaAvailability`, `prepareAgendaTicketSaga`, `reserveMembershipNextSession`, el adaptador interno, las transacciones serializables y sus reintentos, `SchedulerAppointment`, `SchedulerAppointmentMembershipBenefit`, `AgendaSyncEvent`, `AgendaReservation`, `PosAppointment` y `PosMembershipAttendance`.
- Una cortesía doble reserva dos citas canónicas con profesionales distintos cuando existen dos lugares. El caso sin capacidad completa falla antes de persistir ticket o citas parciales.
- El replay de próxima sesión con la misma llave devuelve la misma cita. Un cambio `ARRIVED → ATTENDED` consume una sesión y una repetición obsoleta recibe `409` sin duplicar evento, asistencia o consumo.
- `NO_SHOW` y cancelación dejan el beneficio disponible y `usedSessions = 0`; asistencia deja `usedSessions = 1` y agota únicamente la membresía de una sesión afectada.
- Los estados proyectados quedan `COMPLETED`, `NO_SHOW` y `CANCELED`. La fuente aprobada muestra respectivamente `ASISTIÓ · SESIÓN APLICADA`, `NO LLEGÓ` y `CANCELADA`; `SCHEDULED` se representa como `AGENDADA`.

## PostgreSQL desechable

- Contenedor dedicado: `keysar-rv6-p2-pg-20260916-193714`, PostgreSQL 16 Alpine, enlazado únicamente a `127.0.0.1:55453` durante las pruebas.
- Bases sintéticas: `keysar_rv6_p2` para diagnóstico, `keysar_rv6_p2_final` para matriz dirigida fresca y `keysar_rv6_p2_all` para suite integral fresca.
- Se aplicaron 46/46 migraciones desde cero a las bases finales. No se usó `.env`, seed general, credenciales operativas ni una BD compartida/productiva. El secreto fue generado localmente y no aparece en este informe.
- El contenedor quedó detenido, sin eliminarlo ni borrar datos, para conservar evidencia local recuperable.

## Verificaciones

| Comando                                                                                                                                                                                                         | Resultado | Evidencia                                                                                                |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | -------------------------------------------------------------------------------------------------------- |
| `pnpm --filter @cosmetics/api prisma migrate deploy` con `DATABASE_URL` y `DIRECT_URL` apuntando explícitamente a cada BD local                                                                                 | PASS      | 46/46 migraciones aplicadas desde cero.                                                                  |
| `RUN_DATABASE_TESTS=true DATABASE_URL=<local-final> DIRECT_URL=<local-final> pnpm --filter @cosmetics/api exec vitest run src/pos-scheduler.integration.test.ts src/scheduler-appointments.integration.test.ts` | PASS      | 2 archivos, 5/5 pruebas. Se observó un conflicto serializable esperado y el retry terminó correctamente. |
| `RUN_DATABASE_TESTS=true DATABASE_URL=<local-all> DIRECT_URL=<local-all> pnpm --filter @cosmetics/api test:integration`                                                                                         | PASS      | 5 archivos y 21/21 pruebas; `scheduler-load.integration.test.ts` omitida por su guarda explícita.        |
| `pnpm --filter @cosmetics/api prisma:schemas`                                                                                                                                                                   | PASS      | Ambos schemas sincronizados.                                                                             |
| `pnpm --filter @cosmetics/api prisma:validate` con la BD local explícita                                                                                                                                        | PASS      | Schema Prisma válido.                                                                                    |
| `pnpm --filter @cosmetics/api lint`                                                                                                                                                                             | PASS      | ESLint sin advertencias.                                                                                 |
| `pnpm --filter @cosmetics/api test:unit`                                                                                                                                                                        | PASS      | 25 archivos, 136/136 pruebas.                                                                            |
| `pnpm --filter @cosmetics/api type-check && pnpm --filter @cosmetics/api build`                                                                                                                                 | PASS      | TypeScript y build completos.                                                                            |
| `pnpm --filter @cosmetics/pos type-check && pnpm --filter @cosmetics/pos build:web`                                                                                                                             | PASS      | TypeScript y builds Vite web/Electron completos.                                                         |
| `pnpm pos:visual:manifest`                                                                                                                                                                                      | PASS      | Referencia `12fb804`, 25 pantallas y 208 capturas inventariadas.                                         |
| `git show 12fb8045cc264b565cb6e764d95ad7b2447fbfa1:apps/pos/src/renderer/src/components/AppointmentsView.tsx` y búsqueda dirigida de estados                                                                    | PASS      | La interfaz aprobada contiene los cuatro estados usados; no requirió cambios visibles.                   |

Durante el diagnóstico inicial, las dos pruebas Scheduler con fechas fijas del 2026-09-11 devolvieron `409`; se corrigió el dato temporal de prueba, no el contrato para aceptar citas pasadas. Un primer intento del fixture nuevo omitió la autorización sintética requerida para abrir jornada y se corrigió antes de la matriz final. Al reabrir el contenedor para la última aserción de atomicidad, un intento reconstruyó la URL con un rol incorrecto y otro alcanzó PostgreSQL antes de que terminara de iniciar; ambos fallaron antes de ejecutar casos. Se identificó el rol local correcto, se rotó únicamente su contraseña con un secreto generado y no impreso, y el rerun final volvió a pasar 5/5 dejando el contenedor detenido. Ninguno de esos intentos se presenta como gate aprobado; las matrices de aceptación concluyeron en verde.

## Pendientes y límites

- RV6-P2 no deja pendiente funcional dentro de su criterio.
- RV6-P3 sigue condicionado al uso o conservación de `AGENDA_PROVIDER=http`; no se probó ni mutó infraestructura externa.
- RV6-P1 continúa bloqueado por controles de Producto ausentes para reasignación y estado manual. B01, B02 y B03 no cambiaron y no se atribuyó aprobación al PO.
- No se ejecutó la prueba de carga de Scheduler porque su activación es un gate separado y explícito; la suite integral la reportó como omitida, no como aprobada.

## Continuación

Si se necesita reproducir RV6-P2, levantar un PostgreSQL 16 local dedicado, crear una base nueva, definir de forma explícita `DATABASE_URL`/`DIRECT_URL`, ejecutar `pnpm --filter @cosmetics/api prisma migrate deploy` y luego la matriz dirigida indicada arriba. Para RV6-P3, primero documentar si el proveedor HTTP continúa siendo rollback operativo; no habilitarlo ni ensayar credenciales externas por inferencia.
